// GENERATED by client/scripts/build-docs.mjs. Do not edit.
import type { DocsBundle } from "../types";
export const DOCS: DocsBundle = {"sections":[{"section":"Getting Started","pages":[{"title":"What Is the Ascentix Rules Engine?","section":"Getting Started","order":101,"slug":"what-is-it"},{"title":"Core Concepts","section":"Getting Started","order":102,"slug":"core-concepts"},{"title":"How Rules Run","section":"Getting Started","order":103,"slug":"how-rules-run"},{"title":"Triggers & Channels","section":"Getting Started","order":104,"slug":"triggers-and-channels"},{"title":"Rule Lifecycle","section":"Getting Started","order":105,"slug":"rule-lifecycle"},{"title":"Beta Limitations","section":"Getting Started","order":106,"slug":"beta-limitations"}]},{"section":"Building Rules","pages":[{"title":"Opening the Rule Builder","section":"Building Rules","order":201,"slug":"opening-the-rule-builder"},{"title":"The Hub","section":"Building Rules","order":202,"slug":"the-hub"},{"title":"Creating a New Rule","section":"Building Rules","order":203,"slug":"creating-a-new-rule"},{"title":"Editor Layout","section":"Building Rules","order":204,"slug":"editor-layout"},{"title":"Building Conditions","section":"Building Rules","order":205,"slug":"building-conditions"},{"title":"Comparison Value Sources","section":"Building Rules","order":206,"slug":"comparison-value-sources"},{"title":"Building Actions","section":"Building Rules","order":207,"slug":"building-actions"},{"title":"Filtering a Condition's Child Records","section":"Building Rules","order":208,"slug":"filtering-child-records"},{"title":"Field Mapping","section":"Building Rules","order":209,"slug":"field-mapping"},{"title":"Table Configuration Tree","section":"Building Rules","order":210,"slug":"table-config-tree"},{"title":"Metadata Pickers","section":"Building Rules","order":211,"slug":"metadata-pickers"},{"title":"Validating & Publishing","section":"Building Rules","order":212,"slug":"validating-and-publishing"},{"title":"Saving & Concurrency","section":"Building Rules","order":213,"slug":"saving-and-concurrency"},{"title":"Accessibility & Responsive Layout","section":"Building Rules","order":214,"slug":"accessibility-responsive"}]},{"section":"Administering","pages":[{"title":"Installing, Verifying & Uninstalling","section":"Administering","order":300,"slug":"installation"},{"title":"Security Roles","section":"Administering","order":301,"slug":"security-roles"},{"title":"Runtime Enforcement","section":"Administering","order":302,"slug":"runtime-enforcement"},{"title":"Evaluation Context","section":"Administering","order":303,"slug":"evaluation-context"},{"title":"Troubleshooting","section":"Administering","order":305,"slug":"troubleshooting"}]},{"section":"Developer Reference","pages":[{"title":"Schema Reference","section":"Developer Reference","order":401,"slug":"schema-reference"},{"title":"Custom APIs","section":"Developer Reference","order":402,"slug":"custom-apis"},{"title":"Client Form Library","section":"Developer Reference","order":403,"slug":"client-form-library"},{"title":"Plugin Registration","section":"Developer Reference","order":404,"slug":"plugin-registration"}]}],"pages":{"what-is-it":{"title":"What Is the Ascentix Rules Engine?","section":"Getting Started","order":101,"slug":"what-is-it","html":"<p>The Ascentix Rules Engine is a configurable, table-driven rules engine for\nMicrosoft Dataverse. It handles <strong>server-side data validation</strong> and\n<strong>client-side dynamic form behavior</strong> on a single shared rule model.</p>\n<p>The engine adds ten <code>asx_</code> configuration tables to hold the rules themselves,\nand nothing else. It stores no results and mirrors none of your data: rules are\nevaluated directly against your organization&#39;s <strong>existing</strong> tables.</p>\n<h2>What it does</h2>\n<ul>\n<li><strong>Enforces validation on save.</strong> A rule can block an invalid create, update, or\ndelete before it commits, with a message explaining why.</li>\n<li><strong>Drives form behavior.</strong> A rule can show or hide a field, mark it required, or\ndisplay a message as a user fills out a form, all without custom JavaScript.</li>\n<li><strong>Runs on demand.</strong> A rule can be evaluated manually against a record (saved or\nunsaved) to check its outcome without touching the database.</li>\n</ul>\n<h2>Key ideas</h2>\n<ul>\n<li><strong>Rules are scoped to a table.</strong> Every rule applies to one Dataverse table (for\nexample, <code>account</code> or <code>incident</code>).</li>\n<li><strong>Rules follow a WHEN / THEN shape.</strong> The WHEN side is a tree of <strong>Conditions</strong>\n(grouped with AND/OR); the THEN side is one or more <strong>Actions</strong> that fire based on\nwhether the conditions match.</li>\n<li><strong>Table Configs describe how to traverse related data.</strong> A rule isn&#39;t limited to\nthe fields on the record that triggered it. A Table Config tree tells the engine\nhow to walk out to lookup records and child records so conditions can reference\nthem too.</li>\n<li><strong>Rules move through a Draft → Published lifecycle.</strong> A new rule starts as a\nDraft. Only a Published rule is enforced.</li>\n</ul>\n<h2>Where you author rules</h2>\n<p>Rules are authored visually in the <strong>Rule Builder</strong>. See the <em>Building Rules</em>\nsection for a full walkthrough of the Rule Builder, Table Config editor, and the\nWHEN/THEN authoring experience.</p>","prevSlug":null,"nextSlug":"core-concepts"},"core-concepts":{"title":"Core Concepts","section":"Getting Started","order":102,"slug":"core-concepts","html":"<h2>Rule</h2>\n<p>A <strong>Rule</strong> is the top-level object you author. It is scoped to a single Dataverse\ntable and is the parent of everything else: its condition tree and its actions. A\nrule also declares which <strong>Triggers</strong> invoke it and, optionally, which <strong>Channels</strong>\nit applies to (covered in <em>Triggers &amp; Channels</em>).</p>\n<h2>Conditions (WHEN)</h2>\n<p>The WHEN side of a rule is a tree of <strong>Condition Groups</strong>. Each group combines its\nchildren with a <strong>Logical Operator</strong>, either <strong>And</strong> or <strong>Or</strong>. Each leaf in the\ntree is a <strong>Condition</strong>, which has a <strong>Condition Type</strong>:</p>\n<ul>\n<li><strong>Field Comparison</strong>: compares a field&#39;s value against a literal or another\nfield, using a <strong>Comparison Operator</strong>: Equals, Not Equals, Greater Than, Greater\nThan Or Equal, Less Than, Less Than Or Equal, Contains, Does Not Contain, Is Null,\nIs Not Null.</li>\n<li><strong>Row Count</strong>: counts related child records (optionally filtered) and checks the\ncount against a minimum and/or maximum.</li>\n<li><strong>Regex Match</strong>: tests a field&#39;s value against a regular expression pattern.</li>\n<li><strong>Calculation</strong>: evaluates a math expression (aggregates over a child\ncollection plus arithmetic) and compares the numeric result against a value.</li>\n</ul>\n<p>Whether the group as a whole is satisfied is the rule&#39;s <strong>match</strong> or <strong>no-match</strong>\noutcome, which drives which actions fire.</p>\n<h2>Actions (THEN)</h2>\n<p>The THEN side of a rule is one or more <strong>Actions</strong>. Each action has an <strong>Action\nType</strong>: Set Visible, Set Required, Show Message, Block, Create Record, Update\nRecord, or Delete Record. Each also fires on either <strong>On Match</strong> or <strong>On No Match</strong>\n(its <strong>Fire On</strong> setting). A validation rule typically pairs a <em>Block</em> action with\n<strong>On No Match</strong>; a form-behavior rule typically pairs <em>Set Visible</em> or <em>Set\nRequired</em> with <strong>On Match</strong>.</p>\n<h2>Table Config</h2>\n<p>A rule&#39;s conditions and field-reference values don&#39;t have to stay on the triggering\nrecord. A <strong>Table Config</strong> is a tree, rooted at the rule&#39;s own table, that describes\nhow to traverse outward to related data: a <strong>Lookup Table</strong> node follows a lookup to\na single related record, and a <strong>Child Table</strong> node follows a one-to-many\nrelationship to a set of related records. Conditions and actions reference specific\nnodes in this tree to say <em>which</em> record or rows they&#39;re evaluating. A Table Config\ntree can be shared across multiple rules.</p>\n<h2>Severity &amp; outcomes</h2>\n<p>Severity (<strong>Information</strong>, <strong>Warning</strong>, or <strong>Error</strong>) is set <strong>per action</strong>, not\nper rule. A single rule can carry actions at different severities (a <em>Show Message</em>\nat Warning alongside a <em>Block</em> at Error), and different rules on the same table can\ndisagree about how serious an outcome is. Severity sets the message&#39;s level, not\nwhether the action blocks. A Warning-severity <strong>Block</strong> still blocks and rolls back\nthe save, exactly like an Error-severity one. See <em>Runtime Enforcement</em> for the\nfull mechanics.</p>","prevSlug":"what-is-it","nextSlug":"how-rules-run"},"how-rules-run":{"title":"How Rules Run","section":"Getting Started","order":103,"slug":"how-rules-run","html":"<p>A single rule can be evaluated in three execution contexts, depending on its\n<strong>Triggers</strong>. The <strong>evaluation model is identical across all three contexts</strong>: the\nsame WHEN/THEN logic, the same Table Config traversal, the same condition types.\nOnly the invocation and which action types take effect differ.</p>\n<h2>On create / update / delete (server engine)</h2>\n<p>A plugin step runs synchronously as part of the database operation whenever a\nrecord on the rule&#39;s table is created, updated, or deleted.</p>\n<ul>\n<li><strong>What triggers it:</strong> a Create, Update, or Delete operation against the rule&#39;s\ntable, matching the rule&#39;s <strong>On Create</strong>, <strong>On Update</strong>, or <strong>On Delete</strong>\ntrigger.</li>\n<li><strong>What actions it can apply:</strong> the full server action set, including write and\nblocking actions: <strong>Block</strong>, <strong>Create Record</strong>, <strong>Update Record</strong>, and <strong>Delete\nRecord</strong>. A fired <strong>Block</strong> action prevents the operation entirely and rolls back\nany pending writes; write actions run atomically in the same transaction as the\ntriggering operation.</li>\n</ul>\n<h2>On form (client form library)</h2>\n<ul>\n<li><strong>What triggers it:</strong> the form loading, or a field on the form changing, matching\nthe rule&#39;s <strong>On Form</strong> trigger.</li>\n<li><strong>What actions it can apply:</strong> the client-facing action set (<strong>Set Visible</strong>,\n<strong>Set Required</strong>, and <strong>Show Message</strong>), applied live to the form. Any\nserver-only actions (like Block or the write actions) attached to the same rule\nare evaluated and reported, but never executed from the form; enforcement of\nthose still happens server-side when the record is saved.</li>\n</ul>\n<h2>Manual / on-demand</h2>\n<p>The ad-hoc path for checking a rule&#39;s outcome without changing any data: useful for\nintegrations, admin tools, or testing a rule before publishing it.</p>\n<ul>\n<li><strong>What triggers it:</strong> an explicit call to the <code>asx_RunRules</code> Custom API, matching\nthe rule&#39;s <strong>Manual</strong> trigger (the API&#39;s default trigger filter). The caller\npasses a table name plus an existing record id, unsaved field values as JSON, or\nboth.</li>\n<li><strong>What actions it can apply:</strong> every fired action across all action types is\nreported back to the caller, but <code>asx_RunRules</code> is always <strong>non-enforcing</strong>. Even\na fired <strong>Block</strong> is only reported, never thrown, and write actions are reported\nas a resolved &quot;would write&quot; description rather than executed.</li>\n</ul>","prevSlug":"core-concepts","nextSlug":"triggers-and-channels"},"triggers-and-channels":{"title":"Triggers & Channels","section":"Getting Started","order":104,"slug":"triggers-and-channels","html":"<p>Every rule declares <strong>Triggers</strong> (which events invoke it) and, optionally,\n<strong>Channels</strong> (which origin of write it should respond to). Together these two\nsettings control when a rule fires.</p>\n<h2>Triggers</h2>\n<p><strong>Triggers</strong> is a multi-select field on the rule, and a rule needs <strong>at least one</strong>\nselected. The available values are:</p>\n<ul>\n<li><strong>On Create</strong></li>\n<li><strong>On Form</strong></li>\n<li><strong>Manual</strong></li>\n<li><strong>On Update</strong></li>\n<li><strong>On Delete</strong></li>\n</ul>\n<p>See <em>How Rules Run</em> for what each trigger invokes and which actions apply under it.</p>\n<h2>Trigger Columns</h2>\n<p>When you select <strong>On Update</strong>, an additional optional setting appears:\n<strong>Fire on change of these columns</strong>, a multi-select listing the root table&#39;s\ncolumns.</p>\n<p>By default (no columns selected) the rule&#39;s update firing is driven by the\ncolumns its <strong>conditions</strong> reference, so a rule only re-runs on update when\none of its condition columns changes. Trigger columns are <strong>added</strong> to that\nset: pick a column here and the rule <em>also</em> fires when it changes. Use one when\nthe rule&#39;s actions depend on a column its conditions don&#39;t reference:</p>\n<blockquote>\n<p>A rule on <code>sample_orderline</code> recomputes the order total by summing all line\namounts in an aggregate action, while its conditions only check whether the\norder is locked. The aggregate reads <code>sample_lineamount</code>, so without\n<strong><code>sample_lineamount</code> as a trigger column</strong> the rule never fires when a line\namount is edited and the total goes stale.</p>\n</blockquote>\n<p>Trigger columns only apply to <strong>On Update</strong>. Create and Delete triggers always\nfire on the entire record regardless of what columns changed.</p>\n<h2>Channels</h2>\n<p><strong>Channels</strong> is also a multi-select field, but it&#39;s optional, and it works the\nopposite way from Triggers: <strong>leaving it empty means the rule applies to every\nchannel</strong>. The available values are:</p>\n<ul>\n<li><strong>Standard</strong> covers every origin that is not a portal: a person saving a\nmodel-driven form, the Web API, scripts and CLI tools, integrations,\nconnectors, service principals, and the platform&#39;s own system or\nasynchronous operations.</li>\n<li><strong>Portal</strong> covers writes coming from a Power Pages portal.</li>\n</ul>\n<p>The engine does not try to tell a human apart from an integration: Dataverse does\nnot expose that reliably, since an interactive session can present an application\nid on the write exactly as a service principal does. The one origin signal the\nplatform guarantees is whether the call came through a Power Pages portal.\nChannels gate which <strong>origin</strong> a rule fires on, independent of which table\noperation triggered it.</p>\n<h2>A practical example</h2>\n<p>A validation rule on <code>account</code> blocks a save without a credit limit approver when\nthe credit limit is large, and you want it enforced only on Power Pages\nsubmissions, not on internal users or the nightly integration.</p>\n<ol>\n<li>Set <strong>Triggers</strong> to <strong>On Create</strong> and <strong>On Update</strong>.</li>\n<li>Set <strong>Channels</strong> to <strong>Portal</strong> only.</li>\n<li>The rule blocks a portal submission, but a save from the model-driven app or a\nwrite from the integration (both <strong>Standard</strong>) is not gated by this rule at all.</li>\n</ol>\n<p>The mirror works too: <strong>Standard</strong> only enforces the rule everywhere except\nthe portal.</p>\n<p>To exempt an integration from a rule while still enforcing it on people, use\nthe platform&#39;s own bypass (<code>BypassCustomPluginExecution</code>) rather than a\nchannel. See <em>Security Roles → Administrative bypass</em>.</p>","prevSlug":"how-rules-run","nextSlug":"rule-lifecycle"},"rule-lifecycle":{"title":"Rule Lifecycle","section":"Getting Started","order":105,"slug":"rule-lifecycle","html":"<p>A rule&#39;s <strong>status</strong> determines whether it is enforced; an optional effective window\ndetermines when.</p>\n<h2>Draft / Published / Archived</h2>\n<p>Every rule has a status:</p>\n<ul>\n<li><strong>Draft</strong> is the default status for a new rule. You can edit it freely, and it is\n<strong>never enforced</strong>, no matter what triggers or channels it has selected.</li>\n<li><strong>Published</strong> is the live status. The engine enforces <strong>only Published</strong> rules,\nand only within their effective window, described below. A rule must pass the\nrule validator before it can move from Draft to Published. See <em>Validating &amp;\nPublishing</em> for what the validator checks and how the Rule Builder surfaces\nissues.</li>\n<li><strong>Archived</strong> is a retired status. An Archived rule is kept for history but is\n<strong>never enforced</strong>.</li>\n</ul>\n<h2>Editing and publishing revisions</h2>\n<p>Every rule also has an editable draft. Its status describes enforcement, not whether\nthe draft can be edited. Saving a published rule leaves its active revision running.\nPublishing captures the draft and its complete data-model configuration as a new\nrevision. Shared-model edits take effect independently for each rule when republished.\nBusiness records and caller permissions remain live.</p>\n<p><strong>View published</strong> shows the active definition. Already-open business forms can retain\nthe definition fetched when they loaded; reload them to pick up a new publication.\nThe server uses the active revision for new evaluations.</p>\n<h2>Effective window</h2>\n<p>A rule can optionally carry an effective window:</p>\n<ul>\n<li><strong>Effective From</strong>: the rule is not enforced before this date/time.</li>\n<li><strong>Effective To</strong>: the rule is not enforced after this date/time.</li>\n</ul>\n<p>Editing these values changes the draft schedule. Publish to apply it to enforcement.</p>\n<p>Both are stored in <strong>UTC</strong>, and both are optional independently. Leaving a bound\n<strong>null</strong> leaves that side of the window open. A Published rule with no effective\nwindow set is enforced continuously, subject only to its Triggers and Channels.</p>\n<p>The Rule Builder edits an exact <strong>date and time</strong>, including seconds. Its\n<strong>Schedule timezone</strong> defaults to UTC; choose Local to display and enter values in\nyour browser&#39;s timezone. Switching the display timezone preserves the scheduled\ninstants. The summary shows when enforcement starts and ends.</p>\n<p>The end bound is inclusive and refers to that exact time, not the end of the\nselected calendar day. For an ambiguous local time during a daylight-saving\ntransition, use UTC to specify the intended instant precisely.</p>\n<blockquote>\n<p>A rule outside its effective window, or still in Draft, is loaded and evaluated\nthe same way structurally, but does not apply.</p>\n</blockquote>","prevSlug":"triggers-and-channels","nextSlug":"beta-limitations"},"beta-limitations":{"title":"Beta Limitations","section":"Getting Started","order":106,"slug":"beta-limitations","html":"<p>The Ascentix Rules Engine is in <strong>open beta</strong>. Everything inside the supported\nboundary described below is expected to work as documented. If it doesn&#39;t, please\n<a href=\"https://github.com/ascentix-software/Ascentix-Rules-Engine/issues\">open a GitHub issue</a>.</p>\n<h2>1. What &quot;open beta&quot; means here</h2>\n<p>The engine is <strong>free and Apache-2.0 licensed</strong>, built and maintained by a\nsingle maintainer. During the beta it is intended for <strong>non-production\nenvironments</strong>. There is no SLA and no warranty: the software is provided\nas-is under the Apache-2.0 licence.</p>\n<h2>2. Supported scale and performance budget</h2>\n<p>The supported ceiling for the beta: about <strong>100 published rules per\nenvironment</strong>, and about <strong>5,000 traversed related rows per save</strong>.</p>\n<p>Benchmarking shows that rules within these boundaries evaluate inside the\n<a href=\"https://learn.microsoft.com/en-us/power-apps/developer/data-platform/analyze-performance\">2-second budget Microsoft recommends for synchronous plug-ins</a>.\nThose tests were not run in your environment; real-world timings depend on the\nnumber of other processes registered on the same events and the volume of data.\nBulk operations (<code>CreateMultiple</code>/<code>UpdateMultiple</code>) incur that per-record\nevaluation cost for every record in the batch.</p>\n<p>Beyond this envelope the engine may still work, but it&#39;s outside what the beta\nhas verified. Rule filters are pushed into the Dataverse queries where provably\nsafe, with column-pruned fetches and per-evaluation caching. Deeper performance\nwork (cross-execution caching, trigger/channel query filtering) is not part\nof the beta.</p>\n<h2>3. Traversal row cap: 25,000. Fails, never truncates</h2>\n<p>A single rule evaluation will load at most <strong>25,000 matching rows per\nrelated node</strong>. Exceeding the cap <strong>fails the save with a named error</strong>\n(identifying the node, the cap, and the root table) rather than silently\nevaluating against partial data. Rule filters count toward &quot;matching&quot;: a\nwell-filtered rule works fine against multi-million-row tables as long as the\nrows the rule needs stay under the cap. The cap is fixed during the beta and is\nnot configurable.</p>\n<h2>4. Related-record changes don&#39;t re-fire rules</h2>\n<p>Rules evaluate when the <strong>root table</strong> of the rule is written. Editing a\nrelated record (a child line, a looked-up customer) does <strong>not</strong> re-evaluate\nrules whose conditions read that record. The outcome refreshes on the next\nroot-table write, so put the trigger on the table whose writes should be gated.</p>\n<h2>5. System-context write actions bypass field-level security</h2>\n<p>A rule configured with <strong>System</strong> evaluation context performs its\nCreate/Update/Delete actions as SYSTEM, which bypasses field-level security\nprofiles. Publishing such a rule is privilege-gated: the publisher must hold\nthe org-wide <code>SEC_SYSWRITE_PRIV</code> privilege on each target table. The bypass is\ninherent to system-context writes and is disclosed rather than blocked. Treat\nusers who can publish System-context write rules as customizer-equivalent.</p>\n<h2>6. Enforcement-step drift and recovery</h2>\n<p>The engine generates its enforcement steps automatically as rules are\npublished and unpublished. If steps drift from configuration (e.g. rules were\nchanged while the registration plugin was disabled), the <strong><code>asx_SyncSteps</code>\nCustom API</strong> (<code>Mode=Sync</code>) reconciles every table in one call and reports any\nsteps an administrator deactivated, without re-enabling them.</p>\n<h2>7. Uninstalling requires step cleanup first</h2>\n<p>Generated steps reference the engine&#39;s plugin type, so uninstalling the\nmanaged solution is dependency-blocked until they&#39;re removed. Run\n<code>asx_SyncSteps</code> with <code>Mode=RemoveAll</code> immediately before uninstalling\n(<em>Administering → Installing, Verifying &amp; Uninstalling</em>).</p>\n<h2>8. Portal (Power Pages) channel testing</h2>\n<p>The verification environment has no Power Pages site, so the <strong>Portal</strong> gate is\nproven by exclusion only: a Standard caller (a form save, a Web API write) is\nshown NOT to be blocked by a Portal-only rule, and the resolver&#39;s Portal\nbranch is unit-tested against a faked execution context. A rule <em>firing</em> for a\nreal portal submission has not been exercised live. Verify Portal-gated rules\nin your own environment.</p>\n<h2>9. Browser support</h2>\n<p>The editor and form library are verified on <strong>Chromium-class browsers</strong>\n(Edge, Chrome). Other browsers are untested during the beta.</p>\n<h2>10. No telemetry</h2>\n<p>By design, the engine collects no data from your environments: no usage data,\nno error reports. <strong>A filed issue is the only signal there is.</strong></p>\n<h2>11. No rule transport between environments</h2>\n<p>There is no supported dev→test→prod promotion of rule configuration during the\nbeta, and no GUID-preserving transport path. General-purpose data movement\ntools such as the Configuration Migration Tool or the XrmToolBox Data\nTransporter can move rule records between environments. Neither has been tested\nagainst the engine&#39;s schema, so treat that route as unverified and confirm the\nresults in a sandbox first.</p>\n<h2>12. Upgrade history is short</h2>\n<p>The managed-upgrade path is executed once per release cycle on a fresh\nverification org. Each beta release is verified to upgrade from its immediate\npredecessor only; no upgrade path beyond that is documented.</p>\n<h2>13. How this is verified</h2>\n<p>Unit and contract suites run in CI on every change. Automated and manual\ntesting runs against a live environment each release, along with a scripted\nfresh-org install/verify/uninstall run.</p>\n<h2>14. Localization</h2>\n<p>The engine stores and renders localized rule messages, and each release is\nspot-checked on one non-English base-language org. Localization is not\nsystematically verified across languages during the beta.</p>\n<h2>15. The &quot;apply inverse&quot; flag is reserved</h2>\n<p>The <code>asx_applyinversewhennotfired</code> column exists in the schema and is settable,\non a Set Visible or Set Required action&#39;s classic form and through the API, but\nnothing reads it: no runtime behaviour depends on its value. The Rule Builder\ndoes not show it. It is reserved for possible future use.</p>","prevSlug":"rule-lifecycle","nextSlug":"opening-the-rule-builder"},"opening-the-rule-builder":{"title":"Opening the Rule Builder","section":"Building Rules","order":201,"slug":"opening-the-rule-builder","html":"<p>The <strong>Rule Builder</strong> is the full-page visual editor for rules: conditions,\nactions, and table configuration.</p>\n<h2>Sitemap</h2>\n<p>Open the model-driven app and use the <strong>Authoring</strong> group in the sitemap,\nunder the <strong>Visual Rule Editor</strong> subarea.</p>\n<p><img src=\"asx_/docs/images/02-01-opening-the-rule-builder-01.png\" alt=\"Model-driven app sitemap with an Authoring group (&quot;Visual Rule Editor&quot; selected) above a Configuration group (Rules, Table Configs), and a &quot;Rule Configuration&quot; area switcher at the bottom.\"></p>\n<p>Selecting it opens the <strong>hub</strong> (the &quot;Rules &amp; data model&quot; landing page), where\nyou work with the rules and table configurations that already exist. See\n<em>The Hub</em> for what&#39;s on that page.</p>\n<blockquote>\n<p>The editor only runs inside the app frame: it relies on the app&#39;s client\nAPI for its data access. Don&#39;t try to open it as a bare standalone page;\nit will load but won&#39;t be able to read or save anything.</p>\n</blockquote>","prevSlug":"beta-limitations","nextSlug":"the-hub"},"the-hub":{"title":"The Hub","section":"Building Rules","order":202,"slug":"the-hub","html":"<p>The hub, titled <strong>Rules &amp; data model</strong>, is the Rule Builder&#39;s landing page. It\nhas two tabs: <strong>Rules</strong> and <strong>Table configurations</strong>.</p>\n<p><img src=\"asx_/docs/images/02-02-the-hub-01.png\" alt=\"Rules &amp; data model hub: Rules and Table configurations tabs, a New rule button, search box, Table and Status filters, and a list with one rule &quot;Order total within credit limit&quot; on sample_order, status Draft, triggers On Create/On Form/On Update.\"></p>\n<h2>Rules tab</h2>\n<p>The Rules tab lists every rule with:</p>\n<ul>\n<li><strong>Rule name</strong>, and the table configuration it uses underneath.</li>\n<li><strong>Table</strong>: the table the rule&#39;s root table configuration is built on.</li>\n<li><strong>Status</strong>: Draft, Published, or Archived. See <em>Rule Lifecycle</em> for what\neach status means.</li>\n<li><strong>Triggers</strong>: the events the rule responds to. See <em>Triggers &amp; Channels</em>.</li>\n<li><strong>Actions</strong>: the rule&#39;s action count.</li>\n<li><strong>Modified</strong>: when the rule was last changed, and by whom.</li>\n</ul>\n<p>Clicking a row opens that rule directly in the editor. See <em>Editor Layout</em>\nfor what you land on.</p>\n<p>Above the list sit <strong>New rule</strong> (see <em>Creating a New Rule</em>), <strong>Search rules</strong>,\nand the <strong>Table</strong> and <strong>Status</strong> filters.</p>\n<h2>Table configurations tab</h2>\n<p>The Table configurations tab lists the shared table-config trees. A table\nconfiguration can be reused across multiple rules, so it is managed here\nrather than inside any one rule.</p>","prevSlug":"opening-the-rule-builder","nextSlug":"creating-a-new-rule"},"creating-a-new-rule":{"title":"Creating a New Rule","section":"Building Rules","order":203,"slug":"creating-a-new-rule","html":"<p>A rule starts with the table configuration it will run against.</p>\n<p><img src=\"asx_/docs/images/02-03-creating-a-new-rule-01.png\" alt=\"New rule modal dialog with a Name field, a Data model radio choice (New configuration for a table / Use an existing configuration), a Configuration dropdown, Triggers checkboxes (On Create, On Form, Manual, On Update, On Delete), and Cancel/Create buttons.\"></p>\n<h2>Steps</h2>\n<ol>\n<li>On the hub&#39;s Rules tab, click <strong>New rule</strong>.</li>\n<li>Enter a <strong>Name</strong> for the rule.</li>\n<li>Choose a <strong>Data model</strong>:<ul>\n<li><strong>New configuration for a table</strong>: start a fresh table-config tree. A\n<strong>Configuration name</strong> field appears for the new tree, plus a <strong>Table</strong>\npicker for the root table.</li>\n<li><strong>Use an existing configuration</strong>: reuse a table-config tree that&#39;s\nalready shared by another rule, chosen from a <strong>Configuration</strong> dropdown.</li>\n</ul>\n</li>\n<li>Select at least one <strong>Trigger</strong>: <strong>On Create</strong>, <strong>On Form</strong>, <strong>Manual</strong>,\n<strong>On Update</strong>, or <strong>On Delete</strong>. See <em>Triggers &amp; Channels</em> for what each\none means. <strong>Create</strong> stays disabled until at least one is checked.</li>\n<li>Click <strong>Create</strong>. The new rule opens in the editor, in <strong>Draft</strong> status.</li>\n</ol>\n<h2>New configuration vs. existing configuration</h2>\n<ul>\n<li>Pick <strong>New configuration for a table</strong> when this rule needs a data shape\nno other rule has defined yet: a table config tree naming the table plus\nwhatever lookup/child nodes the rule&#39;s conditions and actions will need to\nreach.</li>\n<li>Pick <strong>Use an existing configuration</strong> when another rule already built the\ntree you need. Table configurations are shared, so reusing one keeps rules\non the same table consistent with each other, and any future extension to\nthat tree (a new node) becomes visible to every rule using it.</li>\n</ul>\n<p>See <em>Editor Layout</em> for how the resulting table-config tree shows up in the\neditor, and <em>Table Configuration Tree</em> for how to extend one.</p>\n<h2>Fire on change of specific columns</h2>\n<p>Once the rule exists, its <strong>Properties</strong> (see <em>Editor Layout</em>) expose a\n<strong>Fire on change of these columns</strong> multi-select, listing the root table&#39;s\ncolumns. This setting applies <strong>only when the On Update trigger is\nselected</strong>.</p>\n<p>By default, an On Update rule only re-fires when a column referenced by its\n<strong>conditions</strong> changes. Trigger columns are <strong>added</strong> to that set: pick a\ncolumn here and the rule <em>also</em> fires when that column changes, even though\nno condition reads it.</p>\n<p>This matters when a rule&#39;s <strong>actions</strong> depend on a column its conditions\ndon&#39;t reference. A rule on <code>sample_orderline</code> uses an aggregate action to\ncompute <code>sum(lines.lineamount)</code> onto the order, but its conditions never\nreference the line amount. Without a trigger column, editing a line&#39;s amount\nwouldn&#39;t re-run the rule, and the computed total would go stale.</p>\n<p>See <em>Triggers &amp; Channels</em> for the full trigger model, and <em>Field Mapping</em>\nfor the aggregate action used in this example.</p>","prevSlug":"the-hub","nextSlug":"editor-layout"},"editor-layout":{"title":"Editor Layout","section":"Building Rules","order":204,"slug":"editor-layout","html":"<p>The editor has four regions: the header, the properties band, the data map,\nand the WHEN/THEN zones that hold the rule body.</p>\n<p><img src=\"asx_/docs/images/02-04-editor-layout-01.png\" alt=\"Rule editor for &quot;Order total within credit limit&quot;: breadcrumb, Save/Reload/Validate/Publish actions, a properties band (Table sample_order, Triggers, Channels All), a DATA MAP row (Orders, sample_customer, sample_orderline, sample_product), a WHEN Execution conditions zone, a WHEN Validation conditions zone with a &quot;Credit check&quot; ALL·AND group and one condition, and a THEN Actions zone with a &quot;Block save&quot; action.\"></p>\n<h2>Header</h2>\n<p>A <strong>breadcrumb</strong> back to the hub, the rule&#39;s <strong>name</strong>, a\n<strong>Draft</strong>/<strong>Published</strong> status badge, and the header actions:</p>\n<ul>\n<li><strong>Edit rule</strong>: creates or reopens a working draft while the published rule stays active.</li>\n<li><strong>Save</strong>: writes draft changes to the server while any published revision stays active.</li>\n<li><strong>Reload</strong>: discards in-memory edits and re-fetches the rule from the\nserver.</li>\n<li><strong>Validate</strong>: runs the rule through the validator without publishing.</li>\n<li><strong>Publish</strong>: validates the saved draft and makes it the next published revision.</li>\n<li><strong>View published / Back to draft</strong>: switches between the frozen definition and the editable draft.</li>\n<li><strong>Restore published to draft</strong>: replaces draft changes after confirmation, without changing enforcement.</li>\n<li><strong>Unpublish</strong>: explicitly stops enforcement.</li>\n</ul>\n<p>See <em>Validating &amp; Publishing</em> for what those checks cover.</p>\n<h2>Properties band</h2>\n<p>The properties band summarizes the rule&#39;s <strong>Table</strong>, <strong>Triggers</strong>, and\n<strong>Channels</strong>. To change them, use the <strong>Properties</strong> inspector, the same panel\nthe condition and action inspectors use: on wide viewports it&#39;s docked\nalongside the editor by default, and on narrow viewports a <strong>Properties</strong>\nbutton appears at the end of the band to bring it up as an overlay.</p>\n<blockquote>\n<p>The properties inspector also exposes settings that don&#39;t fit in the\nresting band: <strong>Fire on change of these columns</strong> (trigger columns),\n<strong>Effective from</strong> / <strong>Effective to</strong>, and <strong>Evaluation context</strong>. See\n<em>Creating a New Rule</em> for trigger columns and <em>Evaluation Context</em> for\nwhat evaluation context controls.</p>\n</blockquote>\n<h2>Data map</h2>\n<p>The <strong>DATA MAP</strong> row shows the rule&#39;s table-config tree: the root table and\nany lookup/child nodes reachable from it. An <strong>Edit data model</strong> link opens\nthe tree for editing. See <em>Table Configuration Tree</em> for how the tree is\nbuilt and extended.</p>\n<h2>WHEN and THEN zones</h2>\n<p>The rule body is organized into three zones:</p>\n<ul>\n<li><strong>WHEN · Execution conditions</strong>: gate whether the rule is evaluated at all.</li>\n<li><strong>WHEN · Validation conditions</strong>: the condition groups (AND/OR trees)\nchecked at save time. Whether they match determines whether the rule&#39;s\nactions fire.</li>\n<li><strong>THEN · Actions</strong>: the actions the rule performs, run in order when the\nrule fires.</li>\n</ul>\n<p>See <em>Building Conditions</em> and <em>Building Actions</em> for how to build out each of these zones.</p>","prevSlug":"creating-a-new-rule","nextSlug":"building-conditions"},"building-conditions":{"title":"Building Conditions","section":"Building Rules","order":205,"slug":"building-conditions","html":"<p>Conditions are what a rule checks. They appear in two zones,\n<strong>WHEN · Execution conditions</strong> and <strong>WHEN · Validation conditions</strong>, which\nuse the same building blocks and differ only in their role in the rule (see\n<em>Editor Layout</em>).</p>\n<h2>Condition groups</h2>\n<p>Conditions live inside <strong>condition groups</strong>, and every group combines its\nchildren one of two ways:</p>\n<ul>\n<li><strong>ALL · AND</strong>: every condition (and subgroup) in the group must match.</li>\n<li><strong>ANY · OR</strong>: at least one condition (or subgroup) in the group must match.</li>\n</ul>\n<p>Groups can <strong>nest</strong>: a group can hold subgroups as well as direct\nconditions, so you can build arbitrarily deep AND/OR trees. An <strong>ANY · OR</strong>\ngroup holding a direct condition and an <strong>ALL · AND</strong> subgroup matches if\neither the direct condition matches, or every condition in the subgroup\ndoes.</p>\n<p><img src=\"asx_/docs/images/02-05-building-conditions-01.png\" alt=\"Validation conditions zone with an &quot;ANY · OR&quot; group &quot;Review triggers&quot; containing a direct condition (sample_approvalnotes Is Null) and a nested &quot;ALL · AND&quot; subgroup &quot;Expedited &amp; high value&quot; with two conditions (sample_isexpedited Equals Yes; sample_ordertotal Greater Than 1000).\"></p>\n<h2>Condition types</h2>\n<p>Each condition is one of four types:</p>\n<ul>\n<li><strong>Field Comparison</strong>: compares a column against a value using a comparison\noperator. The default and most common type.</li>\n<li><strong>Row Count</strong>: counts the related child rows at a child table-config\nnode and checks the count against a <strong>min</strong> and/or <strong>max</strong>. Only valid on\na child node. At Create of the parent record, Row Count evaluates the child\nrows existing at that instant (always zero for the record&#39;s own\ncollections), so a minimum-row rule blocks the create unless <strong>On Create</strong>\nis omitted from its triggers. The validator flags this combination with a\n<code>STRUCT_ROWCOUNT_ON_CREATE</code> warning at authoring time.</li>\n<li><strong>Regex Match</strong>: tests a text column against a regular expression, matching\nor not matching.</li>\n<li><strong>Calculation</strong>: compares a numeric expression against a value using a\nnumeric comparison operator. The expression can aggregate child collection\nvalues and perform arithmetic.</li>\n</ul>\n<h2>Comparison operators</h2>\n<p>For a Field Comparison condition, the <strong>Operator</strong> dropdown offers:</p>\n<ul>\n<li><strong>Equals</strong></li>\n<li><strong>Not Equals</strong></li>\n<li><strong>Greater Than</strong></li>\n<li><strong>Greater Than Or Equal</strong></li>\n<li><strong>Less Than</strong></li>\n<li><strong>Less Than Or Equal</strong></li>\n<li><strong>Contains</strong></li>\n<li><strong>Does Not Contain</strong></li>\n<li><strong>Is Null</strong></li>\n<li><strong>Is Not Null</strong></li>\n</ul>\n<p>The editor only offers operators that suit the comparison column&#39;s data\ntype: ordering operators like Greater Than aren&#39;t offered for a text column,\nand Contains/Does Not Contain aren&#39;t offered for a number column. <code>Is Null</code>\nand <code>Is Not Null</code> are always available and ignore whatever&#39;s configured on\nthe value side.</p>\n<p><code>Is Null</code> matches both an explicitly-null value <strong>and a column that is\nabsent from the record</strong>: Dataverse omits null attributes entirely, so an\nabsent column is how &quot;no value&quot; actually looks at runtime, and <code>Is Null</code>\ntreats it as a match (while <code>Is Not Null</code> treats it as no match).</p>\n<h2>Calculation conditions</h2>\n<p>A Calculation condition evaluates the arithmetic expression held in the\ninspector&#39;s <strong>Calculation</strong> field and compares the result against a value:\n&quot;order total is greater than 1000,&quot; or &quot;average line item discount is less\nthan 5%.&quot; Only the six numeric operators apply (Equals, Not Equals, Greater\nThan, Greater Than Or Equal, Less Than, Less Than Or Equal). The right-hand\nside uses the same <strong>Value source</strong> options as a Field Comparison (Literal,\nField Reference, Text template, or Date calculation); see <em>Comparison Value\nSources</em>.</p>\n<p>Expressions support:</p>\n<ul>\n<li><strong>Column references:</strong> <code>{root.&lt;column&gt;}</code> for the triggering record,\n<code>{node:&lt;tableconfig-guid&gt;.&lt;column&gt;}</code> for related records.</li>\n<li><strong>Numeric literals:</strong> e.g., <code>42</code>, <code>3.5</code>.</li>\n<li><strong>Arithmetic:</strong> <code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>, and <code>(</code> <code>)</code> for grouping.</li>\n<li><strong>Unary minus:</strong> a leading <code>-</code> (e.g., <code>-{root.sample_value}</code>).</li>\n<li><strong>Aggregate functions:</strong> <code>sum(node:&lt;guid&gt;.&lt;column&gt;)</code>,\n<code>avg(node:&lt;guid&gt;.&lt;column&gt;)</code>, <code>min(node:&lt;guid&gt;.&lt;column&gt;)</code>,\n<code>max(node:&lt;guid&gt;.&lt;column&gt;)</code>, and <code>count(node:&lt;guid&gt;)</code> to aggregate over a\nchild collection.</li>\n</ul>\n<p>For example: <code>sum(node:&lt;lines&gt;.lineamount) &gt; 1000</code>.</p>\n<p><strong>Insert field</strong> exposes the related nodes and columns, inserting the\nappropriate token at your cursor. <strong>Insert aggregate</strong> walks you through\nselecting a function (sum, avg, min, max, count), the child collection, and\noptionally the column to aggregate. Both use the same interface as the\nCalculation field-mapping source; see the <strong>Calculation</strong> section in <em>Field\nMapping</em> for detailed examples and guidance on aggregate functions and\noperand types.</p>\n<p><img src=\"asx_/docs/images/02-05-building-conditions-03.png\" alt=\"Condition inspector with Condition type set to Calculation, showing an Expression field (placeholder &quot;e.g. {quantity} * {price}: use Insert field, + - * / and ( )&quot;), Insert field and Insert aggregate buttons, a numeric Operator (Less Than Or Equal), and a Value source.\"></p>\n<p>If a calculation cannot be computed (a null operand, a divide-by-zero, or an\naggregate function returning an empty result such as <code>avg()</code> on an empty\nchild collection), <strong>the condition is not satisfied</strong> and the comparison\ndoes not fire. The same holds if the right-hand side value is missing or\nnull.</p>\n<h2>The condition inspector</h2>\n<p>Selecting a condition opens its inspector. Two of its fields are documented\nnowhere else:</p>\n<ul>\n<li><strong>Table-config node</strong>: which node in the data map tree this condition\nevaluates against (the root, or a lookup/child node reachable from it).</li>\n<li><strong>Comparison column</strong>: the column to evaluate, for Field Comparison and\nRegex Match. Row Count replaces this section with min/max row fields;\nCalculation replaces it with the Calculation expression field.</li>\n</ul>\n<p>The rest are Condition name, Condition type, Operator, Value source (Literal\nby default), and Value.</p>\n<p><img src=\"asx_/docs/images/02-05-building-conditions-02.png\" alt=\"Condition editing panel for &quot;sample_ordertotal &gt; 1000&quot; with fields Condition name, Table-config node (Orders), Condition type (Field Comparison), Comparison column (Order Total), Operator (Greater Than), Value source (Literal), Value (1000).\"></p>\n<p>See <em>Comparison Value Sources</em> for the rest of the value-source options, and\n<em>Building Actions</em> for what happens once a group&#39;s conditions match.</p>","prevSlug":"editor-layout","nextSlug":"comparison-value-sources"},"comparison-value-sources":{"title":"Comparison Value Sources","section":"Building Rules","order":206,"slug":"comparison-value-sources","html":"<p>A Field Comparison condition&#39;s right-hand side doesn&#39;t have to be a fixed\nvalue. The <strong>Value source</strong> dropdown in the condition inspector controls\nwhere it comes from.</p>\n<h2>Literal</h2>\n<p>The default: a typed constant in the <strong>Value</strong> field, such as the <code>1000</code> in\n<code>sample_ordertotal</code> Greater Than <code>1000</code>.</p>\n<h2>Field Reference</h2>\n<p>The right-hand side is <strong>another column</strong>, on the condition&#39;s own record or\non a related table-config node. You might compare the order total against\nthe customer&#39;s credit limit rather than a hardcoded number.</p>\n<p>Choosing Field Reference exposes two additional fields:</p>\n<ul>\n<li><strong>Right-hand node</strong>: the table-config node the right-hand column lives on.\nLeave unset to compare against another column on the condition&#39;s own node.</li>\n<li><strong>Right-hand column</strong>: the column on that node to compare against.</li>\n</ul>\n<blockquote>\n<p>The right-hand node must be <strong>single-cardinality</strong>: the root record or a\nnode reachable through a lookup chain. A node under a one-to-many/child\nrelationship isn&#39;t a valid Right-hand node.</p>\n</blockquote>\n<p><img src=\"asx_/docs/images/02-06-comparison-value-sources-01.png\" alt=\"Condition inspector with Value source set to &quot;Field Reference&quot; plus Right-hand node (sample_customer (lookup)) and Right-hand column (sample_creditlimit), comparing sample_ordertotal to the customer&#39;s credit limit.\"></p>\n<h2>Text template</h2>\n<p>The right-hand side is built from <strong>literal text combined with tokens</strong>:\n<code>{root.&lt;column&gt;}</code> for a column on the condition&#39;s own record, or\n<code>{node:&lt;node&gt;.&lt;column&gt;}</code> for a column on a related table-config node. This is\nthe same token syntax used by Text template field mappings (see <em>Field\nMapping</em>). The template is resolved <strong>at evaluation time</strong>, against the\nrecord actually being checked.</p>\n<p>Choosing Text template exposes a template textarea, an <strong>Insert field</strong> menu,\nand a preview of the rendered template.</p>\n<blockquote>\n<p>A template used as a comparison value is <strong>fail-fast</strong>: an unknown node, a\nmalformed token, or a right-hand node that resolves to more than one record\nstops rule execution with a configuration error instead of comparing\nagainst unrendered text. Message tokens behave differently (see <em>Building\nActions</em> → <em>Dynamic message text</em>).</p>\n</blockquote>\n<h2>Date calculation</h2>\n<p>The right-hand side is a <strong>computed date</strong>: an anchor plus an offset.\nChoosing Date calculation exposes:</p>\n<ul>\n<li><strong>Anchor</strong>: either <strong>&quot;When the rule runs&quot;</strong> or a date column, on the\ncondition&#39;s own record or on a related single-cardinality table-config\nnode.</li>\n<li><strong>Op</strong>: add or subtract.</li>\n<li><strong>Amount</strong>: a positive whole number.</li>\n<li><strong>Unit</strong>: minutes, hours, days, weeks, months, or years.</li>\n</ul>\n<p>The result is a DateTime, compared against the comparison column using the\nsame operators as any other Field Comparison or Calculation condition (see\n<em>Building Conditions</em>). A one-line summary shows the resolved expression, for\nexample &quot;Created On + 3 days&quot;.</p>\n<blockquote>\n<p>If the anchor column is null, isn&#39;t a date, or the anchor node resolves to\nmore than one related record, resolving the comparison fails as a\nconfiguration error rather than silently skipping the condition.</p>\n</blockquote>","prevSlug":"building-conditions","nextSlug":"building-actions"},"building-actions":{"title":"Building Actions","section":"Building Rules","order":207,"slug":"building-actions","html":"<p>Actions are the <strong>THEN</strong> layer of a rule: what happens once the validation\nconditions have been checked.</p>\n<h2>Fire on</h2>\n<p>Every action has a <strong>Fire on</strong> setting:</p>\n<ul>\n<li><strong>On Match</strong>: the action fires when the rule&#39;s validation conditions\nmatch.</li>\n<li><strong>On No Match</strong>: the action fires when they don&#39;t.</li>\n</ul>\n<h2>Action types</h2>\n<ul>\n<li><strong>Set Visible</strong>: shows or hides a form field.</li>\n<li><strong>Set Required</strong>: marks a form field required or not required.</li>\n<li><strong>Show Message</strong>: surfaces a message to the user at a given severity.\nNever blocks a save.</li>\n<li><strong>Block</strong>: prevents the save. Server-enforced.</li>\n<li><strong>Create Record</strong>: creates a new record, with values from a field\nmapping.</li>\n<li><strong>Update Record</strong>: updates a target record, with values from a field\nmapping.</li>\n<li><strong>Delete Record</strong>: deletes a target record.</li>\n</ul>\n<p><strong>Set Visible</strong> and <strong>Set Required</strong> are form actions that use a\n<strong>Target field</strong> to say which field they apply to. <strong>Show Message</strong> and\n<strong>Block</strong> also take an optional <strong>Target field</strong>: set one and the\nnotification (or block) attaches to that field inline; leave it blank and\nit applies at the form level instead: a form banner for Show Message, or\na form-level block for Block. <strong>Show Message</strong> and <strong>Block</strong> carry a\n<strong>Message</strong> plus a <strong>Severity</strong> (Information, Warning, or Error).</p>\n<blockquote>\n<p><strong>What blocks a save is the action type, not the severity.</strong> A <strong>Block</strong>\naction blocks the operation and rolls back at any severity, <strong>Warning</strong>\nincluded. A <strong>Show Message</strong> never blocks on the server, at any severity.</p>\n<p>On a form it is not that simple. A Show Message with a <strong>Target field</strong> is\nshown as a notification on the control, and the platform renders control\nnotifications at Error only, so the platform&#39;s own validation stops the save\nuntil the condition stops matching. That happens whatever severity you set.\nA Show Message with no target field banners and does not block. See\n<em>Runtime Enforcement</em>.</p>\n</blockquote>\n<h2>Dynamic message text</h2>\n<p>A <strong>Block</strong> or <strong>Show Message</strong> action&#39;s <strong>Message</strong> (and its per-language\n<strong>Translations</strong>) can include the same tokens used elsewhere in the editor:\n<code>{root.&lt;column&gt;}</code> for a column on the triggering record, or\n<code>{node:&lt;node&gt;.&lt;column&gt;}</code> for a column on a related table-config node. Tokens\nare rendered <strong>at fire time</strong>, from the record that actually matched. The\nmessage editor has an <strong>Insert field</strong> menu and a preview of the rendered\ntoken.</p>\n<blockquote>\n<p>If a token can&#39;t resolve (an unknown node, a malformed token, a related\nrecord that no longer exists), the message <strong>degrades to the raw,\nunrendered text</strong> rather than failing the rule. A template used as a\ncondition&#39;s comparison value fails fast instead (see <em>Comparison Value\nSources</em>).</p>\n</blockquote>\n<h2>The action inspector</h2>\n<p>Selecting an action opens its inspector, where you configure:</p>\n<ul>\n<li><strong>Action type</strong>: one of the seven types above.</li>\n<li><strong>Active</strong>: a toggle to enable or disable the action without deleting\nit.</li>\n<li><strong>Fire on</strong>: On Match or On No Match.</li>\n<li><strong>Target field</strong>: as described above, required for Set Visible / Set\nRequired and optional for Show Message / Block.</li>\n<li><strong>Value toggle</strong>: for Set Visible / Set Required, the value to apply\nwhen the action fires (on means visible, or required).</li>\n<li><strong>Message</strong>, <strong>Severity</strong> (Information, Warning, or Error), and\n<strong>Translations</strong> (per-language overrides for the message text), for Show\nMessage and Block actions.</li>\n</ul>\n<p>A <strong>WHAT HAPPENS</strong> explainer in the inspector summarizes what the\ncurrently-configured action will do.</p>\n<p><img src=\"asx_/docs/images/02-07-building-actions-01.png\" alt=\"Action editing panel &quot;EDITING ACTION 1 · Block&quot; with Action type (Block), Active toggle, Fire on (On No Match), Target field, Message (&quot;Order total exceeds the customer&#39;s credit limit.&quot;), Severity (Error), and a WHAT HAPPENS explainer.\"></p>\n<p>Create Record and Update Record actions replace the Message/Severity\nfields with a field mapping. See <em>Field Mapping</em> for how to map target\ncolumns to their values.</p>","prevSlug":"comparison-value-sources","nextSlug":"filtering-child-records"},"filtering-child-records":{"title":"Filtering a Condition's Child Records","section":"Building Rules","order":208,"slug":"filtering-child-records","html":"<p>When a condition targets a <strong>child (collection) node</strong>, the condition editor\noffers an <strong>&quot;Only consider records where…&quot;</strong> section. The condition evaluates\nonly the child records that match the filter.</p>\n<h2>How filters work</h2>\n<p>Each filter is a <strong>block that targets one specific node</strong> and specifies an\n<strong>AND/OR set of criteria</strong> on that node&#39;s columns.</p>\n<ul>\n<li><strong>First filter targets the child node</strong>: a new filter starts on the\ncondition&#39;s own child node.</li>\n<li><strong>Add more filters</strong>: each additional filter can target the same child node,\nor an <strong>ancestor node</strong> to filter by that related record&#39;s columns.</li>\n<li><strong>All filters must match</strong>: a child record is evaluated only if it satisfies\n<strong>all</strong> filters you add. An ancestor filter acts as an additional constraint\nvia the parent relationship.</li>\n</ul>\n<p><img src=\"asx_/docs/images/02-08-filtering-child-records-01.png\" alt=\"The &quot;Only consider records where…&quot; filter modal opened from a Row Count condition, with a filter block showing a &quot;FILTER ON (this record&#39;s collection)&quot; node selector, a &quot;Match AND/OR of the following&quot; toggle, a column/operator criteria row, &quot;Add&quot; and &quot;Add filter&quot; buttons, and Cancel/Apply.\"></p>\n<h2>Supported operators</h2>\n<p>A filter&#39;s criteria support the same comparison operators as Field Comparison\nconditions:</p>\n<ul>\n<li><strong>Equals</strong></li>\n<li><strong>Not Equals</strong></li>\n<li><strong>Greater Than</strong></li>\n<li><strong>Greater Than Or Equal</strong></li>\n<li><strong>Less Than</strong></li>\n<li><strong>Less Than Or Equal</strong></li>\n<li><strong>Contains</strong></li>\n<li><strong>Does Not Contain</strong></li>\n<li><strong>Is Null</strong></li>\n<li><strong>Is Not Null</strong></li>\n</ul>\n<p>The editor only offers operators that make sense for the column&#39;s data type.</p>\n<h2>Comparison values: literal or field reference</h2>\n<p>Each criterion&#39;s comparison value can be:</p>\n<ul>\n<li><strong>Literal</strong>: a fixed value you type in.</li>\n<li><strong>Field reference</strong>: read from a related record&#39;s column through a\nlookup-chain or single-record reference. For example, compare a line item&#39;s\ndate against the order&#39;s due date.</li>\n</ul>\n<h2>Examples</h2>\n<h3>Example 1: Filter child records by a column on the child node</h3>\n<p>A rule on <strong>order lines</strong> checks &quot;every line <strong>with status = Active</strong> must have\namount &gt; 0.&quot;</p>\n<ul>\n<li>Create a condition on the order-line node: <code>amount &gt; 0</code>.</li>\n<li>Add a filter targeting the order-line node: <code>status Equals Active</code>.</li>\n<li>Result: only order lines with <code>status = Active</code> are checked.</li>\n</ul>\n<p><img src=\"asx_/docs/images/02-08-filtering-child-records-02.png\" alt=\"The filter modal with a completed criterion: FILTER ON &quot;(this record&#39;s collection)&quot;, Match AND, and a row reading Line Amount (sample_lineamount) · Greater than · Literal · 100, plus Add, Add filter, and Cancel/Apply.\"></p>\n<h3>Example 2: Filter child records by a column on an ancestor node</h3>\n<p>A rule on <strong>order lines</strong> checks &quot;every line must have amount &gt; 0, but only for\norders in the <strong>Northeast region</strong>.&quot;</p>\n<ul>\n<li>Create a condition on the order-line node: <code>amount &gt; 0</code>.</li>\n<li>Add a filter that targets the <strong>parent order node</strong>: <code>region Equals Northeast</code>.</li>\n<li>Result: only lines that belong to a Northeast order are checked.</li>\n</ul>\n<h2>Has related rows… (existence filtering)</h2>\n<p>A filter&#39;s <strong>Add ▾</strong> menu offers <strong>Condition</strong>, <strong>Group</strong>, and a third option:\n<strong>Related-rows filter</strong>. Adding one turns <strong>the criterion</strong> into an existence\ncheck: the criterion is satisfied when another related collection has a <strong>count\nof matching rows</strong> within a range you choose, instead of comparing a single\ncolumn.</p>\n<p>This expresses &quot;this order has at least one line item with status = Active&quot;\ninside the filter, without a separate Row Count condition.</p>\n<h3>Picking the related collection</h3>\n<p>A <strong>&quot;Related rows collection&quot;</strong> picker lists the rule&#39;s child-collection nodes\nfrom the configuration tree. The engine relates the picked collection to the\nnode the filter targets through the <strong>common ancestor</strong> the two nodes share in\nthe tree. A line node and a shipment\nnode that both hang off the same order node are related through that order.</p>\n<h3>Count modes</h3>\n<p>The <strong>&quot;How many matching rows?&quot;</strong> dropdown sets the count range, reusing the\nsame modes as a Row Count condition:</p>\n<ul>\n<li><strong>At least one (exists)</strong>: min 1, no upper bound.</li>\n<li><strong>None (does not exist)</strong>: max 0.</li>\n<li><strong>At least N</strong>: min N, no upper bound.</li>\n<li><strong>At most N</strong>: max N, no lower bound.</li>\n<li><strong>Exactly N</strong>: min and max both N.</li>\n<li><strong>Between N and M</strong>: min N, max M.</li>\n<li><strong>Custom (min / max)</strong>: the raw bounds, set directly.</li>\n</ul>\n<p><img src=\"asx_/docs/images/02-08-filtering-child-records-03.png\" alt=\"The Row Count condition inspector with the &quot;How many matching rows?&quot; dropdown open, listing At least one (exists), None (does not exist), At least N, At most N, Exactly N, Between N and M, and Custom (min / max).\"></p>\n<h3>The sub-filter</h3>\n<p>Once a collection is picked, a <strong>sub-filter</strong> appears below the count mode: an\nAND/OR set of criteria on the related collection&#39;s columns, with the same\noperators and value sources as any other filter. Its <strong>Add</strong> menu offers only\n<strong>Condition</strong> and <strong>Group</strong>: existence filtering supports <strong>one level of\nnesting</strong>.</p>\n<h3>Available in both surfaces</h3>\n<p>&quot;Has related rows…&quot; appears wherever a filter builder does: a\ncondition&#39;s <strong>&quot;Only consider records where…&quot;</strong> section (this page) and an\naggregate&#39;s <strong>&quot;Filter this aggregate…&quot;</strong> builder (see <em>Field Mapping</em> →\n<em>Filtering an aggregate</em>).</p>\n<h3>Example: filter by an existence check on a different collection</h3>\n<p>A rule on <strong>order lines</strong> checks &quot;only consider a line when its <strong>order also\nhas an expedited shipment</strong>.&quot;</p>\n<ul>\n<li>Create a filter on the order-line node.</li>\n<li>Use the <strong>Add</strong> menu and choose <strong>Related-rows filter</strong>.</li>\n<li>Pick the <strong>shipments</strong> collection.</li>\n<li>Set the count mode to <strong>At least one (exists)</strong>.</li>\n<li>In the sub-filter, add <code>type Equals Expedited</code>.</li>\n<li>Result: only order lines whose order has at least one expedited shipment\nare checked.</li>\n</ul>\n<p>Switching the count mode to <strong>None (does not exist)</strong> inverts the criterion to\n&quot;…order has <strong>no</strong> expedited shipment&quot; instead.</p>","prevSlug":"building-actions","nextSlug":"field-mapping"},"field-mapping":{"title":"Field Mapping","section":"Building Rules","order":209,"slug":"field-mapping","html":"<p><strong>Create Record</strong> and <strong>Update Record</strong> actions write values onto the\ntarget record&#39;s columns, configured through the <strong>Map columns</strong> dialog,\nopened from the action&#39;s <strong>&quot;Edit columns…&quot;</strong> button in the action\ninspector.</p>\n<h2>The Map columns dialog</h2>\n<p>The dialog is a <strong>master–detail</strong> layout: a narrow list rail on the left\nand a detail pane on the right.</p>\n<p>The rail lists one entry per mapped target column: its display name, a\nsmall colored dot for the source type, and a one-line <strong>Source · value</strong>\nsummary (for example, &quot;Literal · Handle with priority.&quot;). Selecting an\nentry loads that column into the detail pane.</p>\n<p>The detail pane sets the mapped <strong>Column</strong> and its <strong>Source</strong>:</p>\n<ul>\n<li><strong>Literal</strong>: a constant value, typed in the editor for the target\ncolumn&#39;s type (text box, date picker, choice dropdown, and so on).</li>\n<li><strong>From this record</strong>: a column&#39;s value from the triggering record.</li>\n<li><strong>From related record</strong>: a column&#39;s value from a related table-config\nnode.</li>\n<li><strong>Text template</strong>: literal text combined with <code>{root.&lt;column&gt;}</code> /\n<code>{node:&lt;tableconfig-guid&gt;.&lt;column&gt;}</code> tokens. A live preview beneath the\ntemplate shows the rendered result as you type.</li>\n<li><strong>Date calculation</strong>, for DateTime targets only: an anchor (either &quot;now&quot;\nor a date column) plus or minus an amount and a unit.</li>\n<li><strong>Link to a record</strong>, for lookup-family targets only (Lookup,\nCustomer, or Owner columns): sets the column to a chosen record&#39;s own\nreference rather than one of that record&#39;s column values, for example a\nnote whose Regarding lookup points back at the root record. Pick &quot;this\nrecord&quot; for the root record, or any related table-config node that\nresolves to exactly one record. Stored as\n<code>{ &quot;target&quot;: &quot;objectid&quot;, &quot;source&quot;: &quot;ref&quot;, &quot;node&quot;: &quot;&lt;tableConfigId&gt;&quot; }</code>,\nwhere <code>node</code> is the table-config id of the chosen record (the root\nnode&#39;s id for &quot;this record&quot;).</li>\n<li><strong>Calculation</strong>, for numeric targets only (integer or decimal columns):\nan arithmetic expression over numeric operands. See <em>Calculation</em> below.</li>\n</ul>\n<p><img src=\"asx_/docs/images/02-09-field-mapping-01.png\" alt=\"&quot;Map columns&quot; modal with two mapped columns: Approval Notes via a Text template (&quot;Auto-flagged: expedited order totaling {root.sample_ordertotal}.&quot; with a live preview) and Handling Instructions via a Literal (&quot;Handle with priority.&quot;), plus Add column, Edit as JSON, and Apply/Cancel.\"></p>\n<h2>Adding and removing columns</h2>\n<p><strong>Add column</strong> appends an entry to the rail and selects it. Each column has\nits own <strong>Remove column</strong> button in the detail pane. <strong>Apply</strong> saves the\nmapping back onto the action, <strong>Cancel</strong> discards it.</p>\n<h2>Edit as JSON</h2>\n<p>An <strong>&quot;Edit as JSON&quot;</strong> link in the dialog&#39;s footer switches to a raw JSON\nview of the underlying field-mapping data, the same structure the engine\nstores and executes. Switching back to the list/detail view re-parses\nwhatever&#39;s in the JSON editor.</p>\n<h2>Calculation</h2>\n<p><strong>Calculation</strong> (in the engine code, <code>mathexpr</code>) computes a numeric result\nfrom an arithmetic expression.</p>\n<h3>Writing a calculation expression</h3>\n<p>Expressions use the same token syntax as text templates:</p>\n<ul>\n<li><code>{root.&lt;column&gt;}</code>: a column from the triggering record.</li>\n<li><code>{node:&lt;tableconfig-guid&gt;.&lt;column&gt;}</code>: a column from a related record.</li>\n<li>Numeric literals (e.g., <code>42</code>, <code>3.5</code>).</li>\n</ul>\n<p><strong>Example:</strong> <code>{root.sample_quantity} * {node:&lt;product&gt;.sample_price}</code>\nmultiplies the order quantity by the product&#39;s unit price.</p>\n<h3>Operators and syntax</h3>\n<ul>\n<li><strong>Arithmetic:</strong> <code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>.</li>\n<li><strong>Grouping:</strong> parentheses <code>(</code> and <code>)</code>.</li>\n<li><strong>Unary minus:</strong> a leading <code>-</code> before an operand (e.g., <code>-{root.sample_value}</code>).</li>\n</ul>\n<h3>Target and operand types</h3>\n<ul>\n<li>The <strong>target column</strong> must be numeric (an integer or decimal column).</li>\n<li><strong>Operands</strong> must resolve to numeric values: numeric columns or numeric\nliterals. A non-numeric operand fails during rule execution with\n&quot;calculation operand &#39;X&#39; is not a numeric value&quot;.</li>\n</ul>\n<h3>Nulls, division by zero, and rounding</h3>\n<p>If <strong>any operand is null</strong>, or a <strong>division by zero</strong> occurs during\nevaluation, the target column is <strong>left unchanged</strong>; no write occurs.\nFractional results are rounded to the <strong>nearest whole number</strong> when writing\nto an integer target. Decimal targets preserve the full precision.</p>\n<h3>Aggregates over child collections</h3>\n<p>Calculations can also aggregate values over a <strong>child collection</strong> (a\none-to-many related table), for example an order&#39;s line-item amounts.</p>\n<h4>Aggregate functions</h4>\n<ul>\n<li><strong><code>sum(node:&lt;guid&gt;.&lt;column&gt;)</code></strong>: sums the column&#39;s numeric values across\nall rows in the collection.</li>\n<li><strong><code>avg(node:&lt;guid&gt;.&lt;column&gt;)</code></strong>: averages them.</li>\n<li><strong><code>min(node:&lt;guid&gt;.&lt;column&gt;)</code></strong>: the smallest value in the column.</li>\n<li><strong><code>max(node:&lt;guid&gt;.&lt;column&gt;)</code></strong>: the largest value in the column.</li>\n<li><strong><code>count(node:&lt;guid&gt;)</code></strong>: the number of rows in the collection; takes no\ncolumn.</li>\n</ul>\n<p>On an empty collection, <code>sum</code> and <code>count</code> return <code>0</code>, while <code>avg</code>, <code>min</code>,\nand <code>max</code> have no value: the calculation then produces no value and the\ntarget is left unchanged (no write occurs).</p>\n<h4>Aggregate operand requirements</h4>\n<ul>\n<li>The <strong>collection must be a child collection</strong> (a one-to-many related\ntable picked as a table-config node). Single-related collections or the\nroot record cannot be aggregated.</li>\n<li>The <strong>column being aggregated must be numeric</strong> (integer or decimal).\nText, choice, date, and other non-numeric columns are errors at runtime.</li>\n<li><strong>Null cells are ignored</strong> during aggregation: they contribute nothing to\nthe sum, are skipped when computing average, and do not affect min/max.</li>\n</ul>\n<h4>Building aggregates in the editor</h4>\n<p><strong>Insert aggregate</strong>, in the calculation editor, prompts for <strong>Function</strong>\n(<code>sum</code>, <code>avg</code>, <code>min</code>, <code>max</code>, or <code>count</code>), <strong>Collection</strong> (the child\ntable-config node), and, for every function except <code>count</code>, <strong>Column</strong> (the\nnumeric column to aggregate). The expression is inserted at the cursor\nposition.</p>\n<h4>Composing aggregates with other operands</h4>\n<p>Aggregates are operands, like column references and literals, and combine\nwith arithmetic operators:</p>\n<p><strong>Example:</strong> <code>sum(node:&lt;lines&gt;.lineamount) * (1 + {root.taxrate})</code>\nmultiplies the sum of all line amounts by a tax factor derived from the\ntriggering record&#39;s tax rate.</p>\n<h4>Filtering an aggregate</h4>\n<p>An aggregate can be filtered to reduce only the child rows that match\nspecific criteria. Below the calculation expression, a teal <strong>Aggregates</strong>\ncard shows one chip row per aggregate in your expression: its function,\ncollection, and (for every function except <code>count</code>) column, each as its own\ndropdown. Each row&#39;s <strong>&quot;Only rows where…&quot;</strong> button opens the filter builder\nused for condition filters, where you add AND/OR criteria on the child\ncollection&#39;s columns. A <strong>Clear</strong> button appears next to it once a filter is\nset.</p>\n<p>Criteria support the same operators as condition filters:</p>\n<ul>\n<li>Equals, not equals, greater than, greater than or equal, less than, less\nthan or equal.</li>\n<li>Contains, does not contain.</li>\n<li>Is null / Is not null.</li>\n</ul>\n<p>A criterion compares against a <strong>literal value</strong> or <strong>reads from another\nrecord</strong> (for example, a line&#39;s amount against the order&#39;s maximum\nthreshold). The filter&#39;s <strong>Add</strong> menu also offers a <strong>Related-rows filter</strong>,\nthe existence check that tests whether a <em>different</em> related collection has\na matching count of rows: only sum lines whose order also has an expedited\nshipment, for instance. See <em>Filtering a Condition&#39;s Child Records</em> → <em>Has\nrelated rows… (existence filtering)</em>.</p>\n<p><strong>Example:</strong> on an order, set a field to <code>sum(node:&lt;lines&gt;.amount)</code> where\neach line&#39;s <code>status</code> equals &quot;Active&quot;. Only active lines contribute to the\nsum.</p>\n<p>A filter that matches no rows behaves like an empty collection.</p>\n<p>Filtering does not change when the rule fires or which other actions run.\nOnly <strong>aggregates</strong> can be filtered; single-record operands like\n<code>{root.…}</code> and <code>{node:…}</code> cannot.</p>\n<p>See <em>Building Actions</em> for how Create Record and Update Record fit among\nthe other action types.</p>","prevSlug":"filtering-child-records","nextSlug":"table-config-tree"},"table-config-tree":{"title":"Table Configuration Tree","section":"Building Rules","order":210,"slug":"table-config-tree","html":"<p>A <strong>table configuration</strong> (also called a <strong>data map</strong> or <strong>traversal\ntree</strong>) describes how the engine reaches related data starting from a\n<strong>root table</strong> out to whatever related tables a rule needs to look at.\nEvery condition and action node picker in the earlier pages, and the\n<strong>DATA MAP</strong> row shown in <em>Editor Layout</em>, are built on top of one of\nthese trees.</p>\n<h2>Node types</h2>\n<p>Every node is one of three types:</p>\n<ul>\n<li><strong>Root Table</strong>: the tree&#39;s starting point, and the table the rule\nitself is bound to. There&#39;s exactly one per configuration.</li>\n<li><strong>Lookup Table</strong>: a <strong>many-to-one</strong> related table, reached through a\nparent lookup column on the current node&#39;s table (for example, an order\nreaching the customer it belongs to via its customer lookup).</li>\n<li><strong>Child Table</strong>: a <strong>one-to-many</strong> related table, reached through a\nchild link field back to the current node.</li>\n</ul>\n<p>Indentation in the tree shows <strong>traversal depth</strong>. A lookup reached from\nunderneath a child node gives a multi-level path, as in\nOrder → Order Line → Product, where Product hangs off the Order Line\n<strong>child</strong> node rather than off the root.</p>\n<p><img src=\"asx_/docs/images/02-10-table-config-tree-01.png\" alt=\"Table configuration &quot;Orders&quot; (SHARED, root sample_order, 4 nodes, used by 6 rules) with a Traversal tree: ROOT Orders, LOOKUP sample_customer, CHILD sample_orderline, LOOKUP sample_product, each with &quot;Add related&quot;, a legend (lookup = many-to-one, child = one-to-many; indentation = depth), and a node-editing panel.\"></p>\n<h2>Configurations are shared</h2>\n<p>A table configuration is <strong>shared and reusable</strong>: more than one rule can\npoint at the same root configuration. Editing a config&#39;s tree (adding a\nrelated node, for example) affects <strong>every rule</strong> that uses it. The\neditor&#39;s header carries a <strong>SHARED</strong>\nmarker and a used-by rule count alongside the root table and node total.</p>\n<h2>Selecting a node</h2>\n<p>Clicking a node in the tree opens its editing panel, showing:</p>\n<ul>\n<li><strong>Node name</strong>: an editable label for the node.</li>\n<li><strong>Table</strong>: the Dataverse table the node resolves to.</li>\n<li><strong>Relationship</strong>: how this node is reached from its parent (for\nexample, &quot;Lookup via parent column <code>sample_customerid</code>&quot; or a child link\nfield).</li>\n<li><strong>Reach from here</strong>: the full path from the root down to this node.</li>\n</ul>\n<p>Every node also exposes an <strong>&quot;Add related&quot;</strong> action, which attaches a\nfurther Lookup or Child node underneath it and is how multi-level paths\nget built, and a <strong>Delete node</strong> action, which removes a node and its\nsubtree. The <strong>Root</strong> node can&#39;t be deleted.</p>\n<p><img src=\"asx_/docs/images/02-10-table-config-tree-02.png\" alt=\"Table config editor with the sample_customer lookup node selected; the Editing node panel shows Node name, Table (sample_customer), Relationship (&quot;Lookup via parent column sample_customerid&quot;), a &quot;Reach from here&quot; path (Orders → sample_customer), and Add related / Delete node.\"></p>\n<h2>Opening a table configuration</h2>\n<p>You can reach the table configuration editor three ways:</p>\n<ul>\n<li><strong>Edit data model</strong>: from a rule&#39;s DATA MAP row, opens that rule&#39;s\nroot configuration.</li>\n<li><strong>Table configurations</strong>: a tab on the hub, listing every\nconfiguration.</li>\n<li><strong>Table Configs</strong>: an entry in the app sitemap.</li>\n</ul>\n<p>See <em>Metadata Pickers</em> for how the &quot;Add related&quot; relationship list is\nkept limited to relationships that actually exist, and <em>Building\nConditions</em> for how a condition picks which node in the tree it\nevaluates against.</p>","prevSlug":"field-mapping","nextSlug":"metadata-pickers"},"metadata-pickers":{"title":"Metadata Pickers","section":"Building Rules","order":211,"slug":"metadata-pickers","html":"<p>Throughout the editor, wherever you&#39;re asked to choose a table, column,\nrelationship, choice, or option-set value, the picker is populated from\n<strong>live Dataverse metadata</strong> rather than a fixed list, scoped to what\nyou&#39;re editing: comparison columns come from the condition&#39;s table-config\nnode, relationships from that node&#39;s table, choice values from the column\nbeing compared. A reference that does slip past the pickers (for example,\na comparison column deleted from the table after a condition was built\naround it) is caught by validation; see <em>Validating &amp; Publishing</em> for how\nthose metadata-aware checks work.</p>\n<h2>Add related, live</h2>\n<p>The table-config <strong>&quot;Add related from here&quot;</strong> picker is built from the\nnode table&#39;s actual relationships, each labeled with a lookup or child\nicon.</p>\n<p><img src=\"asx_/docs/images/02-11-metadata-pickers-01.png\" alt=\"The &quot;Add related from here&quot; picker on the sample_customer node listing real relationships from that table&#39;s metadata: sample_customer (lookup), userentityinstancedata (child), sample_customer (child), sample_order (child), with lookup/child icons.\"></p>\n<p>See <em>Table Configuration Tree</em> for how these relationship picks build out\na traversal tree, and <em>Building Conditions</em> / <em>Comparison Value Sources</em>\nfor the column and field-reference pickers that draw on the same\nmetadata.</p>\n<h2>Searching pickers</h2>\n<p>The <strong>Table</strong> and <strong>Column</strong> pickers throughout the editor are\ntype-to-filter comboboxes: start typing to narrow the list by display\nname or logical name (&quot;Type to filter tables&quot; / &quot;Type to filter columns&quot;\nplaceholders). Each also has a <strong>&quot;Custom tables only&quot;</strong> / <strong>&quot;Custom columns\nonly&quot;</strong> checkbox that hides system metadata.</p>\n<h2>Browsing for a record</h2>\n<p>Lookup value fields, a condition&#39;s Literal value in <em>Building Conditions</em>\nand a field mapping&#39;s Literal value in <em>Field Mapping</em>, show a\n<strong>Browse…</strong> button next to the inline search box. Browse… opens the\n<strong>Record Picker</strong> modal:</p>\n<ul>\n<li>A <strong>view selector</strong>: pick one of the table&#39;s saved views; the chosen\nview sets the base filter and which columns appear in the results grid.</li>\n<li><strong>In-view text search</strong>: narrows the view&#39;s results by name as you type.</li>\n<li>An <strong>advanced filter</strong>: a nested AND/OR filter builder, the same\nbuilding blocks used for condition groups, letting you filter by any\ncolumn on the table rather than just the primary name.</li>\n<li>A <strong>paged results grid</strong>, built from the selected view&#39;s columns, with a\n<strong>Load more</strong> button to fetch additional pages. Selecting a row and\nclicking <strong>Select</strong> resolves the lookup.</li>\n</ul>\n<p>The inline quick-search stays available for fast typing on small tables.\nBrowse… is there for tables with many records, duplicate names, or when you\nneed to filter by something other than the primary name.</p>","prevSlug":"table-config-tree","nextSlug":"validating-and-publishing"},"validating-and-publishing":{"title":"Validating & Publishing","section":"Building Rules","order":212,"slug":"validating-and-publishing","html":"<p>Publishing is <strong>gated</strong> on validity: a <strong>Draft</strong> rule only becomes\n<strong>Published</strong> if it validates cleanly. <strong>Validate</strong> reports what is\nstanding in the way.</p>\n<p>When there are unsaved edits, the button reads <strong>Save &amp; validate</strong>: it first saves\nthe Draft, then validates the persisted result. On a clean rule it reads <strong>Validate</strong>.\nPublished rules remain active while you edit and save their draft. Validate the\ndraft, then <strong>Publish</strong> to replace the live revision. Validation checks the captured\nconfiguration, including shared data models. If it changes before publication,\npublication is rejected and you must validate again. See <em>Saving &amp; Concurrency</em>.</p>\n<h2>Validation layers</h2>\n<p>Validation runs three layers of checks, in order, collecting every issue\nfound rather than stopping at the first one:</p>\n<ul>\n<li><strong>Structural</strong>, the rule&#39;s basic shape: it has at least one condition\nand one action, no condition group is empty, and every condition/action\nhas the fields its type requires.</li>\n<li><strong>Traversal integrity</strong>: every table-config node a condition or action\npoints at exists in the rule&#39;s tree and is reachable from the root, and\nsingle-cardinality requirements (like a Field Reference right-hand node)\nare respected.</li>\n<li><strong>Metadata-aware</strong>: every referenced table and column actually exists,\nand is creatable, updatable, or readable as needed for how it&#39;s used\n(for example, a Create Record mapping target must be creatable; a\ncomparison column must be readable).</li>\n</ul>\n<p>All three layers report <strong>errors</strong>, which block publishing. Issues can also\ncarry a <strong>Warning</strong> severity, which is advisory and does not block: a Row\nCount minimum that can never pass at Create (<code>STRUCT_ROWCOUNT_ON_CREATE</code>),\nand the privilege requirement stated for a gated System-context write action\n(<code>SEC_SYSWRITE_REQ</code>).</p>\n<h2>Reading the issues panel</h2>\n<p>Each issue shows a <strong>code</strong>, a human-readable <strong>message</strong>, and the\n<strong>field or node</strong> it applies to. Errors are also marked <strong>inline</strong> on the\noffending row.</p>\n<p>Below, a condition&#39;s <strong>Comparison column</strong> and an action&#39;s <strong>Target\ncolumn</strong> both reference columns that no longer exist, raising two\n<code>META_COLUMN_NOT_FOUND</code> errors.</p>\n<p><img src=\"asx_/docs/images/02-12-validating-and-publishing-01.png\" alt=\"Rule editor showing &quot;Validation found 2 issues&quot; and a Validation Issues panel with two META_COLUMN_NOT_FOUND errors (a condition&#39;s ComparisonColumn and an action&#39;s TargetColumn), an inline error on the condition row, and a disabled Publish button.\"></p>\n<p>Once every issue is resolved, Validate shows a green <strong>&quot;Validation\npassed. The rule is valid&quot;</strong> banner and <strong>Publish</strong> becomes available.</p>\n<p><img src=\"asx_/docs/images/02-12-validating-and-publishing-02.png\" alt=\"Rule editor showing a green &quot;Validation passed. The rule is valid&quot; banner and an enabled blue Publish button on a Draft rule.\"></p>\n<h2>Publishing is enforced</h2>\n<p>Publishing creates a numbered revision and enables the rule. Republishing a live\nrule atomically replaces its active revision. Only\n<strong>Published</strong> rules are enforced at runtime; a Draft rule, however valid,\ndoesn&#39;t run. See <em>Rule Lifecycle</em> for the full Draft/Published/Archived\nflow, and <em>Runtime Enforcement</em> for how a published rule&#39;s actions are\nactually applied.</p>","prevSlug":"metadata-pickers","nextSlug":"saving-and-concurrency"},"saving-and-concurrency":{"title":"Saving & Concurrency","section":"Building Rules","order":213,"slug":"saving-and-concurrency","html":"<h2>Dirty tracking</h2>\n<p>While your in-progress edits differ from what was last loaded or saved,\nthe header shows an <strong>unsaved changes</strong> indicator and the <strong>Save</strong> button\nis enabled.</p>\n<h2>Save is atomic</h2>\n<p><strong>Save</strong> persists the entire rule graph (the rule itself, its condition\ngroups and conditions, and its actions) in <strong>one atomic transaction</strong>.</p>\n<h2>Reload</h2>\n<p><strong>Reload</strong> discards your in-memory edits and re-fetches the rule fresh\nfrom the server, picking up any changes someone else made.</p>\n<h2>Undo, redo, and browser recovery</h2>\n<p><strong>Undo</strong> restores the previous edit, including a deleted group and its children.\n<strong>Redo</strong> reapplies it. The editor keeps the last 100 edits; a successful save or\nreload starts a new history.</p>\n<p>Pending edits also have a recovery copy in the current browser tab. After a page\nreload, choose <strong>Restore edits</strong> to recover them, or <strong>Discard recovery</strong> to remove\nthat copy. Restoring never saves or publishes a rule. Recovery is scoped to the\nenvironment and rule, and lasts only for the browser tab&#39;s session. If storage is\nunavailable, the editor says so. Explicitly discarding edits clears their recovery\ncopy.</p>\n<p><strong>Review changes</strong> shows pending operations and lets you <strong>Copy changes</strong> before\nreloading. If clipboard access is unavailable, select and copy the text manually.</p>\n<h2>Optimistic concurrency</h2>\n<p>Saving uses <strong>optimistic concurrency</strong>: if the rule was changed elsewhere\nsince you loaded it, your save is rejected and you&#39;re told to reload\nbefore saving again. Your in-progress edits stay in memory; the server&#39;s\ncopy is untouched. Use <strong>Review changes</strong> to copy your pending changes, reload the\ncurrent version, and reapply the changes you still want. Recovery retains the\noriginal version checks; restoring an old edit does not override someone else&#39;s\nwork.</p>\n<h2>Editing a published rule</h2>\n<p>Choose <strong>Edit rule</strong> on a published rule to create or reopen its <strong>working draft</strong>. Save as often as needed while the last\npublished revision continues enforcing. <strong>Publish</strong> validates the saved draft and\nreplaces the active revision. A rejected publication leaves the active revision intact.</p>\n<p><strong>View published</strong> opens the frozen definition, including its data model, read-only.\n<strong>Back to draft</strong> returns to your work, preserving unsaved edits. <strong>Restore published\nto draft</strong> asks you to confirm replacing both saved and unsaved draft changes. It\ncreates a private copy of the published data model so other rules&#39; shared models\nare unaffected. You must publish again to change enforcement.</p>\n<p>API callers open the working draft before editing its records. Direct changes to published configuration are rejected. Shared-model\nchanges affect a published rule only after that rule is republished. <strong>Unpublish</strong>\nremains an explicit way to stop enforcement; it is not required for editing.</p>\n<h2>No autosave</h2>\n<p>Every change stays local until you click <strong>Save</strong> or <strong>Save &amp; validate</strong>.</p>","prevSlug":"validating-and-publishing","nextSlug":"accessibility-responsive"},"accessibility-responsive":{"title":"Accessibility & Responsive Layout","section":"Building Rules","order":214,"slug":"accessibility-responsive","html":"<p>The editor is keyboard-operable and labelled for assistive technology\nthroughout, and its layout reflows rather than forcing horizontal\nscrolling.</p>\n<h2>Keyboard and screen reader support</h2>\n<p>Every interactive element in the editor (tree rows, condition and\naction rows, buttons, and the breadcrumb) is keyboard-operable, with\naccessible names and labels for assistive technology. Status regions like\nthe validation issues panel are announced as they update, so keyboard and\nscreen-reader users can drive the same workflows described in this guide\nwithout a mouse.</p>\n<h2>Responsive layout</h2>\n<p>The editor&#39;s layout <strong>reflows on narrower viewports</strong> rather than forcing\nhorizontal scrolling:</p>\n<ul>\n<li><strong>Header actions</strong>: the Save / Reload / Validate / Publish cluster\nwraps below the rule name instead of crowding it.</li>\n<li><strong>The data map</strong>: the DATA MAP row&#39;s chips wrap onto additional lines\nas the tree&#39;s node list grows too wide for one row.</li>\n<li><strong>Condition and action rows</strong>: long condition and action rows (for\nexample, an operator plus a field-reference value) stack their pieces\nvertically instead of truncating.</li>\n<li><strong>Side panels</strong>: the node inspector and other side panels move below\nthe tree rather than squeezing into a shrinking column beside it.</li>\n</ul>\n<p><img src=\"asx_/docs/images/02-14-accessibility-responsive-01.png\" alt=\"The rule editor at a narrow width with the Save/Reload/Validate/Publish buttons wrapped under the rule name, the data-map chips wrapped to a second line, and a condition row stacking its operator and field-reference value.\"></p>","prevSlug":"saving-and-concurrency","nextSlug":"installation"},"installation":{"title":"Installing, Verifying & Uninstalling","section":"Administering","order":300,"slug":"installation","html":"<h2>Before you install</h2>\n<ul>\n<li>A Dataverse environment. During the beta, use a <strong>non-production</strong>\nenvironment (see <em>Beta Limitations §1</em>).</li>\n<li><strong>System Administrator</strong> on that environment to import the solution.</li>\n<li>The managed solution zip,\n<code>AscentixRulesEngine_0.0.0.N_managed.zip</code> (N = the beta\nnumber), from the\n<a href=\"https://ascentix.ca/power-platform/rules-engine/download\">download page</a>,\nwhich also lists the file&#39;s SHA-256. Only the managed package is\nsupported.</li>\n</ul>\n<h2>Install</h2>\n<ol>\n<li>Open the <a href=\"https://admin.powerplatform.microsoft.com\">Power Platform admin center</a>\n→ your environment → <strong>Solutions</strong> (or <a href=\"https://make.powerapps.com\">make.powerapps.com</a>\n→ <strong>Solutions</strong>) and choose <strong>Import solution</strong>.</li>\n<li>Select the managed zip and import. No settings or connection references\nare prompted. The engine has no external dependencies.</li>\n</ol>\n<p>Everything ships in the solution: the rule configuration tables, the plug-in\nassembly with its bootstrap registration steps, the Custom APIs, the model-driven app, the Rule Builder web resources, and the two security roles.\nExisting rules work after import. Open a published rule and choose <strong>Edit rule</strong>\nto create or resume its working draft. The published rule continues enforcing\nuntil you publish the draft. Internal coordination and configuration preservation\nare handled automatically; no migration command or customer pipeline is required.</p>\n<h2>Assign roles</h2>\n<p>Assign the shipped roles (see <em>Security Roles</em> for exactly what they grant):</p>\n<ul>\n<li><strong>Rules Engine Author</strong>: for people who create and edit rules.</li>\n<li><strong>Rules Engine Reader</strong>: for people (or service accounts) that only read\nrule configuration.</li>\n</ul>\n<p>Administrators need no extra role. Publishing a <strong>System-context</strong> rule with\nwrite actions requires the publisher to hold the matching privileges org-wide\non each target table (<em>Evaluation Context</em>).</p>\n<h2>Post-import verification checklist</h2>\n<ol>\n<li><strong>Solution present:</strong> the solution list shows <em>Ascentix Rules Engine</em>\nat the version you installed, managed.</li>\n<li><strong>App opens:</strong> launch the <strong>Ascentix Rules Engine</strong> model-driven\napp; the sitemap shows the Authoring and Configuration groups.</li>\n<li><strong>Hub loads:</strong> open the Rule Builder. The hub renders its Rules and Table\nconfigurations tabs, both empty, without errors.</li>\n<li><strong>Author a test rule:</strong> in the hub, create a rule on any test table with\na new configuration; add one condition that a test record will violate\nand a <strong>Block</strong> action (fire on <strong>On No Match</strong>) with a recognizable\nmessage; triggers <strong>On Create</strong>.</li>\n<li><strong>Validate &amp; publish:</strong> Validate shows no errors; Publish succeeds.</li>\n<li><strong>Enforcement is live:</strong> create a violating record → the save is blocked\nwith <em>&quot;This record could not be saved:&quot;</em> and your message. Fix the value\n→ the save succeeds.</li>\n<li><strong>Report-only works:</strong> call <code>asx_RunRules</code> for the table and a record id\n(see <em>Custom APIs</em>). It returns a verdict without writing anything.</li>\n<li><strong>Unpublish releases:</strong> set the rule back to Draft → the previously\nblocked save now succeeds.</li>\n<li><strong>Clean up:</strong> delete the test rule and configuration; delete the test\n records.</li>\n</ol>\n<p>If any step fails,\n<a href=\"https://github.com/ascentix-software/Ascentix-Rules-Engine/issues\">open a GitHub issue</a>.</p>\n<h2>Upgrading</h2>\n<p>Import the newer managed zip over the installed one (the default <strong>Upgrade</strong>\nbehavior). Published rules, their configurations, and their generated\nenforcement steps are unaffected. Each beta release is verified to upgrade\nfrom its immediate predecessor (<em>Beta Limitations §12</em>); don&#39;t skip versions\nwithout testing in a sandbox first.</p>\n<h2>Uninstalling</h2>\n<p>The engine&#39;s generated enforcement steps live outside the\nsolution (they&#39;re created at publish time in your environment) and reference\nthe engine&#39;s plug-in type, so a straight solution delete is\n<strong>dependency-blocked</strong> while any exist.</p>\n<ol>\n<li>Call the <strong><code>asx_SyncSteps</code></strong> Custom API with <code>Mode = &quot;RemoveAll&quot;</code> (System\nAdministrator or System Customizer; see <em>Custom APIs</em>). This deletes every\nengine-generated step, including any you deactivated. It is recoverable:\nyour rules are not touched.</li>\n<li>Delete the managed solution from the Solutions list. Rules, configurations\nand the engine&#39;s tables are removed with it. <strong>This is not recoverable.</strong></li>\n</ol>\n<p>If you deleted the solution first and got a dependency error naming\n<code>Ascentix.RulesEngine</code> steps, run step 1, then delete again. If you run\n<code>RemoveAll</code> but <em>don&#39;t</em> uninstall, one <code>asx_SyncSteps</code> call with\n<code>Mode = &quot;Sync&quot;</code> (or any rule publish) regenerates the steps from your\npublished rules.</p>","prevSlug":"accessibility-responsive","nextSlug":"security-roles"},"security-roles":{"title":"Security Roles","section":"Administering","order":301,"slug":"security-roles","html":"<p>Two solution-owned security roles control who can work with rules and their\nconfiguration in Dataverse.</p>\n<h2>The two roles</h2>\n<table>\n<thead>\n<tr>\n<th>Role</th>\n<th>Tables</th>\n<th>Privileges</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Rules Engine Author</strong></td>\n<td>the 10 config tables (<code>asx_rule</code>, <code>asx_conditiongroup</code>, <code>asx_rulecondition</code>, <code>asx_tableconfig</code>, <code>asx_searchcriteriagroup</code>, <code>asx_searchcriterion</code>, <code>asx_nodefiltergroup</code>, <code>asx_nodefiltercriterion</code>, <code>asx_ruleaction</code>, <code>asx_localizedmessage</code>)</td>\n<td>Create, Read, Write, Delete, Append, AppendTo</td>\n</tr>\n<tr>\n<td><strong>Rules Engine Reader</strong></td>\n<td>the same 10 config tables</td>\n<td>Read</td>\n</tr>\n</tbody></table>\n<p>Both roles are <strong>additive</strong>: Dataverse unions privileges across a user&#39;s\nroles, so assign either one on top of whatever roles a user already has. All\nprivileges are at <strong>Organization</strong> depth, because rule configuration is\norg-wide reference data.</p>\n<h2>Who can author vs. publish</h2>\n<p>There is no separate &quot;publisher&quot; role. Moving a rule from <strong>Draft</strong> to\n<strong>Published</strong> is a <strong>Write</strong> on <code>asx_rule</code>, so anyone holding <strong>Rules Engine\nAuthor</strong> can both author and publish. What gates a rule from actually reaching\nPublished is not a security role but the rule <strong>validator</strong>: publishing is\nblocked until the rule passes validation, regardless of who is doing the\npublishing. See <em>Rule Lifecycle</em> for what Draft, Published, and Archived mean\nat runtime.</p>\n<p>Beyond the privileges in the table: Authors need no privileges on Dataverse\nplatform tables like <code>sdkmessageprocessingstep</code>, because the engine&#39;s own\nregistration plugin runs as the system user and handles step registration on\ntheir behalf. Reader is for a user who needs to see rules without editing them.\nNo part of the engine&#39;s runtime requires it: the enforcement plugin,\n<code>asx_RunRules</code> and <code>asx_ReadRules</code> all read configuration as system (see\nbelow), so the client form library reaches configuration without it. The\ncomponent that reads configuration in the calling user&#39;s own context is the\nRule Builder, whose users hold Author.</p>\n<h2>Rule-config reads run as system</h2>\n<p><code>RulesEnginePlugin</code> loads the config tables (<code>asx_rule</code>, <code>asx_conditiongroup</code>,\n<code>asx_rulecondition</code>, <code>asx_tableconfig</code>, <code>asx_ruleaction</code>, and the\nsearch/node-filter tables) using the <strong>system user</strong> service, not the\ncalling user&#39;s. Without that, a caller lacking read access to <code>asx_rule</code> would\nload zero rules and enforcement would silently not apply. Because the plugin\nalways reads config as system, a user&#39;s Reader/Author role assignment has no\nbearing on whether rules are enforced for that user&#39;s writes. It only controls\nwhether <em>that user</em> can read or edit the configuration directly (through a\nview, the Rule Builder, or the <code>asx_RunRules</code>/<code>asx_ReadRules</code> APIs).</p>\n<p>The <strong>business data</strong> a rule&#39;s conditions traverse is a separate concern,\ncontrolled per rule by the <strong>Evaluation Context</strong> setting rather than by these\ntwo roles. See <em>Evaluation Context</em>.</p>\n<h2>Administrative bypass</h2>\n<p>The engine has no on/off switch. To run a migration or bulk import without\nrule enforcement, admins use platform-native mechanisms instead:</p>\n<ul>\n<li>An admin holding the <code>prvBypassCustomBusinessLogic</code> privilege can set\n<strong><code>BypassCustomPluginExecution</code></strong> on a request (from the SDK, Configuration\nMigration, or bulk import tooling) to skip custom plugins (including this\nengine) for that operation.</li>\n<li><strong>Channels</strong> is not an integration bypass. Its only distinction is\n<strong>Portal</strong> versus <strong>Standard</strong>, because Dataverse does not reliably tell a\nhuman apart from an application user. See <em>Triggers &amp; Channels</em>. Use\n<code>BypassCustomPluginExecution</code> to exempt an integration.</li>\n<li>A rule can be kept in <strong>Draft</strong> or <strong>Archived</strong>, or scheduled outside its\neffective window, to suppress it without deleting it. See <em>Rule\nLifecycle</em>.</li>\n</ul>","prevSlug":"installation","nextSlug":"runtime-enforcement"},"runtime-enforcement":{"title":"Runtime Enforcement","section":"Administering","order":302,"slug":"runtime-enforcement","html":"<p>What a published rule does when it fires depends on <em>which action type</em> fired\nand <em>where</em> the rule ran, not on severity alone.</p>\n<h2>Severity is a message level, not a block switch</h2>\n<p>Severity (<strong>Information</strong>, <strong>Warning</strong>, or <strong>Error</strong>) is set per action\n(see <em>Core Concepts</em>) and controls the notification level a message is\nshown at. It does <strong>not</strong>, by itself, determine whether an operation is\nblocked: on the server a <strong>Show Message</strong> is always informational, at any\nseverity. On a form, a Show Message targeted at a field does stop the save,\nfor the reason given under <em>On form</em> below.</p>\n<p>What blocks an operation is the action <strong>type</strong>. Only a fired <strong>Block</strong>\naction stops the save, and severity is not consulted when deciding whether to\nblock: a <strong>Warning</strong>-severity <code>Block</code> blocks and rolls back the save exactly\nlike an Error-severity one.</p>\n<h2>On create / update / delete (server, authoritative)</h2>\n<p>When a Create, Update, or Delete against a rule&#39;s table matches the rule&#39;s\ntrigger:</p>\n<ul>\n<li>The plugin evaluates every applicable rule and dispatches its fired\nactions.</li>\n<li>If <strong>any</strong> <code>Block</code> action fires (across any rule on that record), the\nplugin throws before any writes happen. <strong>No</strong> part of the operation\ncommits, including any <code>Create Record</code> / <code>Update Record</code> / <code>Delete Record</code> write actions that also fired: <strong>block wins</strong>.</li>\n<li>If no <code>Block</code> fires, any fired write actions (<code>Create Record</code>, <code>Update Record</code>, <code>Delete Record</code>) are applied atomically in the same transaction\nas the triggering operation. A write failure throws and rolls back the\nwhole operation.</li>\n<li>Write actions don&#39;t cascade into rules triggered by their own writes; they\nexecute only at the top level of the operation.</li>\n<li>This holds identically across all three triggers, including <strong>On\nDelete</strong>: a <code>Block</code> that fires when a matching record is deleted throws\nbefore the delete is applied, and the row survives.</li>\n<li>Enforcement also respects the rule&#39;s <strong>Channels</strong> gate (see <em>Triggers &amp;\nChannels</em>): a write through a Power Pages portal resolves to <strong>Portal</strong>,\nevery other origin to <strong>Standard</strong>, and each is enforced (or excluded) as\nsuch. The engine does not distinguish a human from an integration.</li>\n</ul>\n<p><strong>Message format.</strong> The exception message aggregates <strong>every</strong> fired\n<code>Block</code> message across all rules on the record as a deduped, bulleted list\nunder a header, for example:</p>\n<pre><code>This record could not be saved:\n • Name must be Valid.\n • Amount must be positive.\n</code></pre>\n<p>A <code>Block</code> action with no configured message falls back to a default block\nmessage. On a bulk operation, the plugin evaluates <strong>every</strong> record rather\nthan failing fast, and throws once with every failing record&#39;s messages and\nid included, so the caller sees every problem in a single round trip.</p>\n<h2>On form (client, advisory)</h2>\n<p>The client form library never calls <code>preventDefault</code> on a save. It has no <code>OnSave</code>\nhandler and only <em>surfaces</em> what a rule would do, giving the person filling\nout the form early feedback before they save:</p>\n<ul>\n<li><code>Set Visible</code> and <code>Set Required</code> are applied directly to the form.</li>\n<li>A <strong>form-level</strong> <code>Show Message</code> sets a form notification at its configured\nseverity and does not block.</li>\n<li>A <strong>field-targeted</strong> <code>Show Message</code> is added to the control instead. The\nplatform renders control notifications at Error only, and an Error control\nnotification stops the save through native field validation. So a Show\nMessage on a field blocks until its condition stops matching, whatever\nseverity it carries. Use a form-level Show Message for advice that must not\nstop anyone.</li>\n<li>A <strong>field-targeted</strong> <code>Block</code> shows as an inline notification on that\nfield only. On save, the platform&#39;s own validation rolls that\nnotification up to the form header and stops the save at that point,\nthrough native field validation rather than through the rules engine.</li>\n<li>A <strong>form-level <code>Block</code></strong> (not targeted at a specific field) shows as a\nform banner instead, since there&#39;s no field to attach it to.</li>\n</ul>\n<p>The server backstops the client for any write that doesn&#39;t go through this\nform (bulk import, API calls, other integrations).</p>\n<h2>Manual / on-demand</h2>\n<p>A rule invoked through <code>asx_RunRules</code> never blocks, regardless of what\nfires (see <em>How Rules Run</em>). Every fired action, including <code>Block</code>, is\nreported back to the caller as data instead: the response&#39;s <code>IsValid</code> flag\nis <code>true</code> only when no <code>Block</code> action fired, and <code>FailedRuleCount</code> counts\nthe distinct rules that fired one. Nothing is thrown and no write actually\nhappens.</p>","prevSlug":"security-roles","nextSlug":"evaluation-context"},"evaluation-context":{"title":"Evaluation Context","section":"Administering","order":303,"slug":"evaluation-context","html":"<p>Every rule has an <strong>Evaluation Context</strong> setting that controls whose read\naccess governs the business data a rule&#39;s conditions traverse: <code>User</code> (the\ndefault) or <code>System</code>.</p>\n<h2>What it governs</h2>\n<p>A Table Config tree lets a rule follow lookups and child relationships out to\nrelated records (see <em>Core Concepts</em>). Reading that data (the triggering\nrecord itself and everything reached through the Table Config) runs under one\nof two contexts:</p>\n<ul>\n<li><strong>User (default, value 1)</strong>: the traversal reads data in the <strong>calling\nuser&#39;s</strong> context, so a rule sees only the data that specific user can\nread. This preserves caller-visibility: the rule&#39;s outcome never exposes\na field or related record the caller couldn&#39;t otherwise see. It also means\na rule&#39;s outcome depends on who is saving: a Row Count minimum can pass for\na read-restricted user where it would block an administrator, and the\nreverse.</li>\n<li><strong>System (value 2)</strong>: the traversal reads data as the <strong>system user</strong>,\nregardless of what the calling user can see.</li>\n</ul>\n<p>This is scoped to <strong>business data</strong> only. It&#39;s independent of how the\nrules engine reads its own configuration: rule definitions (<code>asx_rule</code> and\nthe other config tables) are always loaded via the system user, no matter\nwhich Evaluation Context a rule declares. See <em>Security Roles</em>.</p>\n<p>Evaluation Context also determines which identity <strong>write actions</strong> run\nas. A fired <code>Create Record</code>, <code>Update Record</code>, or <code>Delete Record</code> action\nexecutes under the same context as the rule that fired it: a User-context\nrule writes as the calling user, a System-context rule writes as the\nsystem user.</p>\n<h2>When to use each</h2>\n<ul>\n<li><strong>Stay on User</strong> for most validation and form-behavior rules.</li>\n<li><strong>Switch to System</strong> when a rule&#39;s traversal needs to reach related data\nthat the calling user may not have read access to: for example, a\nvalidation that must check a field on a related record regardless of the\ncaller&#39;s per-record security, or a write action that needs to succeed\neven though the calling user lacks write privileges on the action&#39;s\ntarget table or column.</li>\n</ul>\n<p>Evaluation Context is set <strong>per rule</strong>, so one rule can stay caller-scoped\nwhile another on the same table is deliberately elevated to System for a\ntraversal or write the caller has no direct access to.</p>","prevSlug":"runtime-enforcement","nextSlug":"troubleshooting"},"troubleshooting":{"title":"Troubleshooting","section":"Administering","order":305,"slug":"troubleshooting","html":"<p>Every fix on this page touches only <em>engine</em> configuration. Nothing here\nwrites to your business tables.</p>\n<h2>A rule is blocking saves it shouldn&#39;t</h2>\n<p>A blocked save shows <strong>&quot;This record could not be saved:&quot;</strong> followed by the\nrule&#39;s message. That message is configured on the rule&#39;s action, so it\nidentifies the rule:</p>\n<pre><code>GET /api/data/v9.2/asx_ruleactions?$select=asx_message\n    &amp;$expand=asx_rule($select=asx_name)\n    &amp;$filter=contains(asx_message,&#39;part of the message text&#39;)\n</code></pre>\n<p>Advanced Find on the <strong>Rule Actions</strong> table, filtered on <em>Message</em> contains,\nreturns the same record.</p>\n<p>Set that rule back to <strong>Draft</strong>. Unpublishing removes or narrows the table&#39;s\nenforcement steps in the same transaction, so the next save is evaluated\nwithout the rule.</p>\n<p>If the rule itself cannot be edited, deactivate its generated enforcement\nstep instead (<em>Plugin Registration</em> covers how the steps are named). That\nsuspends enforcement for the whole table and message, not just the one rule.\nReconciliation and <code>asx_SyncSteps</code> leave a deactivated step alone until an\nadministrator reactivates it.</p>\n<h2>The Rule Builder won&#39;t open, or shows an error panel</h2>\n<p>When the editor can&#39;t start it renders a panel (<strong>&quot;The editor could not start.&quot;</strong>\nor <strong>&quot;The rule could not load.&quot;</strong>) with the error text and a <strong>Reload</strong>\nbutton. Work through these in order:</p>\n<ol>\n<li><strong>Reload.</strong> Press the panel&#39;s Reload button. A transient metadata or token\nfailure clears on the next load.</li>\n<li><strong>Open it from the app, not the raw URL.</strong> The Rule Builder only runs\ninside the model-driven app, where it gets its client API and data\ncontext. A web-resource URL pasted into the address bar loads the page with\nnothing to talk to, and the panel says so (&quot;The editor must be opened from\nwithin a model-driven app&quot;). Open the\n<strong>Ascentix Rules Engine</strong> app and use <strong>Authoring → Visual Rule\nEditor</strong> (<em>Opening the Rule Builder</em>).</li>\n<li><strong>Content Security Policy.</strong> The editor loads nothing from outside your\nenvironment (scripts, styles, and fonts all ship as web resources), and\neach release is verified on an org with CSP enforcement turned on. If your\nenvironment adds its own CSP directives and the editor still fails, open\nthe browser console (F12): CSP violations are reported there by name.\nInclude that line in your report.</li>\n<li><strong>Browser.</strong> The editor and form library are verified on Chromium-class\nbrowsers (Edge, Chrome). Other browsers are untested during the beta\n(<em>Beta Limitations §9</em>); if you&#39;re on one, try Edge or Chrome before\nreporting.</li>\n</ol>\n<p>If the panel keeps coming back, copy its error text verbatim.</p>\n<h2>Enforcement is a few seconds behind a publish</h2>\n<p>Publishing a rule registers or widens the table&#39;s enforcement steps as part\nof the publish itself, but the platform can take a few seconds to start\nrouting saves through a newly registered step. If the first save after a\npublish goes through when it shouldn&#39;t have, wait and save again before\ntreating it as a bug.</p>\n<h2>Uninstall says dependencies exist</h2>\n<p>Deleting the managed solution while engine-generated enforcement steps still\nexist is dependency-blocked. The error looks like this (ids will differ):</p>\n<pre><code>Solution dependencies exist, cannot uninstall. DependencyCount : 2\nRequiredComponentObject details: Type: PluginType,\n  ObjectName: Ascentix.RulesEngine.Plugin.RulesEnginePlugin, Id: …\nDependentComponentObject details: Type: SdkMessageProcessingStep, Id: …\nDependencyType: Published\n</code></pre>\n<p><code>DependencyCount</code> is the number of engine-generated steps still registered.\nClear them with the <strong><code>asx_SyncSteps</code></strong> Custom API at <code>Mode = &quot;RemoveAll&quot;</code>,\nthen delete the solution again. The full sequence is in the <em>Uninstalling</em>\nsection of <em>Installing, Verifying &amp; Uninstalling</em>.</p>\n<h2>Collect diagnostics before you report</h2>\n<p>The engine collects no telemetry from your environment: by design, nothing\nphones home (<em>Beta Limitations §10</em>). A report has to carry its own evidence:</p>\n<ul>\n<li><strong>Solution version.</strong> From the environment&#39;s <strong>Solutions</strong> list: the\nversion shown for <em>Ascentix Rules Engine</em>.</li>\n<li><strong>The exact block message text</strong>, copied verbatim from the save error, if\na save was blocked.</li>\n<li><strong>The rule name and the table</strong> it targets. Both are on the rule&#39;s row in\nthe hub.</li>\n<li><strong>Whether it reproduces with <code>asx_RunRules</code>.</strong> Call it for the table and\nrecord (<em>Custom APIs</em>). It reports what fired without writing anything.\nPass <code>IncludeDiagnostics: true</code> and include the <code>Diagnostics</code> output: it\ncarries the evaluation&#39;s timings and row counts, which is usually enough\nto show where the time or the rows went.</li>\n<li><strong>For editor problems:</strong> the browser and version, and any errors from the\nbrowser console (F12 → Console), including the error panel&#39;s text.</li>\n<li><strong>The environment&#39;s base language</strong>, especially if it isn&#39;t English.\nLocalization is spot-checked on one non-English org per release, not\nsystematically (<em>Beta Limitations §14</em>).</li>\n</ul>\n<p>Don&#39;t include record data you wouldn&#39;t want outside your organization; rule\nnames, messages, and diagnostics are enough.</p>\n<h2>Report a problem</h2>\n<p>Bugs and questions go to <strong>GitHub Issues</strong>:</p>\n<p><a href=\"https://github.com/ascentix-software/Ascentix-Rules-Engine/issues\">github.com/ascentix-software/Ascentix-Rules-Engine/issues</a></p>\n<p>Open a new issue, pick the <strong>Bug report</strong> or <strong>Question</strong> template, and paste\nin the diagnostics above. If the report contains something you can&#39;t put in\na public issue, email <a href=\"mailto:info@ascentix.ca\">info@ascentix.ca</a> instead. The in-app help viewer&#39;s\n<strong>Report a problem</strong> link opens the same issue tracker in a new tab.</p>\n<p><strong>A suspected security vulnerability does not go in a public issue.</strong> Report\nit privately: on the repository, use the <strong>Security</strong> tab →\n<a href=\"https://github.com/ascentix-software/Ascentix-Rules-Engine/security/advisories/new\"><strong>Report a vulnerability</strong></a>,\nor email <a href=\"mailto:info@ascentix.ca\">info@ascentix.ca</a> with <code>SECURITY</code> in the\nsubject line. The full policy is in the repository&#39;s <code>SECURITY.md</code>.</p>","prevSlug":"evaluation-context","nextSlug":"schema-reference"},"schema-reference":{"title":"Schema Reference","section":"Developer Reference","order":401,"slug":"schema-reference","html":"<p>The rule model is stored as ordinary Dataverse records in a small set of\n<code>asx_</code>-prefixed tables. The default publisher prefix is <code>asx</code>, and every logical name\nbelow assumes it. Every table carries the standard <code>asx_name</code> primary column in\naddition to the columns listed here. For what these objects mean, see <em>Core\nConcepts</em>.</p>\n<h2>Global choices</h2>\n<p>All choices are <strong>global</strong> and single-select unless marked multi-select. Values are\nstable integers. Do not assume label text; match on the value.</p>\n<table>\n<thead>\n<tr>\n<th>Choice</th>\n<th>Schema name</th>\n<th>Values</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Logical Operator</td>\n<td><code>asx_logicaloperator</code></td>\n<td>And = 1, Or = 2</td>\n</tr>\n<tr>\n<td>Table Config Type</td>\n<td><code>asx_tableconfigtype</code></td>\n<td>Root Table = 1, Lookup Table = 2, Child Table = 3</td>\n</tr>\n<tr>\n<td>Condition Type</td>\n<td><code>asx_conditiontype</code></td>\n<td>Field Comparison = 1, Row Count = 2, Regex Match = 3, Calculation = 4</td>\n</tr>\n<tr>\n<td>Comparison Operator</td>\n<td><code>asx_comparisonoperator</code></td>\n<td>Equals = 1, Not Equals = 2, Greater Than = 3, Greater Than Or Equal = 4, Less Than = 5, Less Than Or Equal = 6, Contains = 7, Does Not Contain = 8, Is Null = 9, Is Not Null = 10</td>\n</tr>\n<tr>\n<td>Severity</td>\n<td><code>asx_severity</code></td>\n<td>Information = 1, Warning = 2, Error = 3</td>\n</tr>\n<tr>\n<td>Action Type</td>\n<td><code>asx_actiontype</code></td>\n<td>Set Visible = 1, Set Required = 2, Show Message = 3, Block = 4, Create Record = 5, Update Record = 6, Delete Record = 7</td>\n</tr>\n<tr>\n<td>Action Fire On</td>\n<td><code>asx_actionfireon</code></td>\n<td>On Match = 1, On No Match = 2</td>\n</tr>\n<tr>\n<td>Triggers <em>(multi-select)</em></td>\n<td><code>asx_triggers</code></td>\n<td>On Create = 1, On Form = 2, Manual = 3, On Update = 4, On Delete = 5</td>\n</tr>\n<tr>\n<td>Channel <em>(multi-select)</em></td>\n<td><code>asx_channel</code></td>\n<td>Standard = 1, Portal = 2</td>\n</tr>\n<tr>\n<td>Comparison Value Source</td>\n<td><code>asx_comparisonvaluesource</code></td>\n<td>Literal = 1, Field Reference = 2, Template = 3, Date Expression = 4</td>\n</tr>\n<tr>\n<td>Evaluation Context</td>\n<td><code>asx_evaluationcontext</code></td>\n<td>User = 1, System = 2</td>\n</tr>\n<tr>\n<td>Criterion Type</td>\n<td><code>asx_criteriontype</code></td>\n<td>Comparison = 1, Exists = 2</td>\n</tr>\n</tbody></table>\n<h2>Core tables</h2>\n<h3>Rule (<code>asx_rule</code>)</h3>\n<p>The top-level object. Lifecycle uses the standard Dataverse <code>statecode</code>/<code>statuscode</code>\npair rather than a custom field. See <em>Rule Lifecycle</em> for what Draft, Published, and\nArchived mean.</p>\n<table>\n<thead>\n<tr>\n<th>Column</th>\n<th>Schema name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Table Logical Name</td>\n<td><code>asx_tablelogicalname</code></td>\n<td>Text</td>\n<td>Yes</td>\n<td>The entity the rule applies to</td>\n</tr>\n<tr>\n<td>Triggers</td>\n<td><code>asx_triggers</code></td>\n<td>Choice (multi) → <code>asx_triggers</code></td>\n<td>Yes</td>\n<td>At least one</td>\n</tr>\n<tr>\n<td>Channels</td>\n<td><code>asx_channels</code></td>\n<td>Choice (multi) → <code>asx_channel</code></td>\n<td>No</td>\n<td>Empty means all channels</td>\n</tr>\n<tr>\n<td>Effective From</td>\n<td><code>asx_effectivefrom</code></td>\n<td>DateTime (UTC)</td>\n<td>No</td>\n<td>Null = open start</td>\n</tr>\n<tr>\n<td>Effective To</td>\n<td><code>asx_effectiveto</code></td>\n<td>DateTime (UTC)</td>\n<td>No</td>\n<td>Null = open end</td>\n</tr>\n<tr>\n<td>Evaluation Context</td>\n<td><code>asx_evaluationcontext</code></td>\n<td>Choice → <code>asx_evaluationcontext</code></td>\n<td>No</td>\n<td>Default User</td>\n</tr>\n<tr>\n<td>Root Table Config</td>\n<td><code>asx_roottableconfig</code></td>\n<td>Lookup → <code>asx_tableconfig</code></td>\n<td>Yes</td>\n<td>The root of the rule&#39;s Table Config tree</td>\n</tr>\n<tr>\n<td>Trigger Columns</td>\n<td><code>asx_triggercolumns</code></td>\n<td>Multiline Text (4000)</td>\n<td>No</td>\n<td>JSON array of root-table column logical names; OnUpdate only. The rule also fires when one of these columns changes (unioned with the columns its conditions reference)</td>\n</tr>\n</tbody></table>\n<h3>Table Config (<code>asx_tableconfig</code>)</h3>\n<p>A node in the traversal tree. Self-referential via Parent Table.</p>\n<table>\n<thead>\n<tr>\n<th>Column</th>\n<th>Schema name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Table Logical Name</td>\n<td><code>asx_tablelogicalname</code></td>\n<td>Text</td>\n<td>Yes</td>\n<td>Dataverse entity logical name</td>\n</tr>\n<tr>\n<td>Table Config Type</td>\n<td><code>asx_tableconfigtype</code></td>\n<td>Choice → <code>asx_tableconfigtype</code></td>\n<td>Yes</td>\n<td>Root / Lookup / Child</td>\n</tr>\n<tr>\n<td>Parent Table</td>\n<td><code>asx_parenttable</code></td>\n<td>Lookup → <code>asx_tableconfig</code></td>\n<td>No</td>\n<td>Self-referential</td>\n</tr>\n<tr>\n<td>Lookup Column Logical Name</td>\n<td><code>asx_lookupcolumnlogicalname</code></td>\n<td>Text</td>\n<td>No</td>\n<td>Lookup Table nodes only</td>\n</tr>\n<tr>\n<td>Child Link Field</td>\n<td><code>asx_childlinkfield</code></td>\n<td>Text</td>\n<td>No</td>\n<td>Child Table nodes only</td>\n</tr>\n<tr>\n<td>Lookup Target Id Attribute</td>\n<td><code>asx_lookuptargetidattribute</code></td>\n<td>Text</td>\n<td>No</td>\n<td>Lookup Table nodes only: the target table&#39;s primary-id attribute</td>\n</tr>\n</tbody></table>\n<h3>Condition Group (<code>asx_conditiongroup</code>)</h3>\n<p>An AND/OR node in a rule&#39;s condition tree (self-referential).</p>\n<table>\n<thead>\n<tr>\n<th>Column</th>\n<th>Schema name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Rule</td>\n<td><code>asx_rule</code></td>\n<td>Lookup → <code>asx_rule</code></td>\n<td>Yes</td>\n<td>Parent rule</td>\n</tr>\n<tr>\n<td>Parent Condition Group</td>\n<td><code>asx_parentconditiongroup</code></td>\n<td>Lookup → <code>asx_conditiongroup</code></td>\n<td>No</td>\n<td>Self-referential</td>\n</tr>\n<tr>\n<td>Logical Operator</td>\n<td><code>asx_logicaloperator</code></td>\n<td>Choice → <code>asx_logicaloperator</code></td>\n<td>Yes</td>\n<td>And / Or</td>\n</tr>\n<tr>\n<td>Is Execution Condition</td>\n<td><code>asx_isexecutioncondition</code></td>\n<td>Yes/No</td>\n<td>Yes</td>\n<td>Marks a group as a rule gate, evaluated before the validation groups</td>\n</tr>\n</tbody></table>\n<h3>Rule Condition (<code>asx_rulecondition</code>)</h3>\n<p>A single leaf check inside a Condition Group.</p>\n<table>\n<thead>\n<tr>\n<th>Column</th>\n<th>Schema name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Condition Group</td>\n<td><code>asx_conditiongroup</code></td>\n<td>Lookup → <code>asx_conditiongroup</code></td>\n<td>Yes</td>\n<td>Parent group</td>\n</tr>\n<tr>\n<td>Table Config</td>\n<td><code>asx_tableconfig</code></td>\n<td>Lookup → <code>asx_tableconfig</code></td>\n<td>Yes</td>\n<td>Which node this condition evaluates</td>\n</tr>\n<tr>\n<td>Condition Type</td>\n<td><code>asx_conditiontype</code></td>\n<td>Choice → <code>asx_conditiontype</code></td>\n<td>Yes</td>\n<td>Field Comparison / Row Count / Regex Match / Calculation</td>\n</tr>\n<tr>\n<td>Comparison Column</td>\n<td><code>asx_comparisoncolumn</code></td>\n<td>Text</td>\n<td>No</td>\n<td>Field to evaluate (also the target column for Regex Match)</td>\n</tr>\n<tr>\n<td>Comparison Operator</td>\n<td><code>asx_comparisonoperator</code></td>\n<td>Choice → <code>asx_comparisonoperator</code></td>\n<td>No</td>\n<td>Field Comparison only</td>\n</tr>\n<tr>\n<td>Comparison Value</td>\n<td><code>asx_comparisonvalue</code></td>\n<td>Multiline text</td>\n<td>No</td>\n<td>Literal value; for Regex Match, the pattern</td>\n</tr>\n<tr>\n<td>Min / Max Expected Rows</td>\n<td><code>asx_minexpectedrows</code> / <code>asx_maxexpectedrows</code></td>\n<td>Whole Number</td>\n<td>No</td>\n<td>Row Count only</td>\n</tr>\n<tr>\n<td>Comparison Value Source</td>\n<td><code>asx_comparisonvaluesource</code></td>\n<td>Choice → <code>asx_comparisonvaluesource</code></td>\n<td>No</td>\n<td>Blank = Literal; Template and Date Expression store their payload in <code>asx_comparisonvalue</code> (template text / date-expression JSON) rather than the node/column fields</td>\n</tr>\n<tr>\n<td>Comparison Value Node</td>\n<td><code>asx_comparisonvaluenode</code></td>\n<td>Lookup → <code>asx_tableconfig</code></td>\n<td>No</td>\n<td>Field Reference right-hand node (see <em>Value nodes</em> below)</td>\n</tr>\n<tr>\n<td>Comparison Value Column</td>\n<td><code>asx_comparisonvaluecolumn</code></td>\n<td>Text</td>\n<td>No</td>\n<td>Field Reference right-hand column</td>\n</tr>\n<tr>\n<td>Condition Expression</td>\n<td><code>asx_conditionexpression</code></td>\n<td>Multiline Text (4000)</td>\n<td>No</td>\n<td>LHS math expression (mathexpr) for a Calculation condition: aggregates over child collections + arithmetic, compared by a numeric operator to the RHS</td>\n</tr>\n</tbody></table>\n<h3>Rule Action (<code>asx_ruleaction</code>)</h3>\n<p>The outcome layer. Columns not relevant to a given Action Type are left blank.</p>\n<table>\n<thead>\n<tr>\n<th>Column</th>\n<th>Schema name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Rule</td>\n<td><code>asx_rule</code></td>\n<td>Lookup → <code>asx_rule</code></td>\n<td>Yes</td>\n<td>Parent rule</td>\n</tr>\n<tr>\n<td>Action Type</td>\n<td><code>asx_actiontype</code></td>\n<td>Choice → <code>asx_actiontype</code></td>\n<td>Yes</td>\n<td>What to do</td>\n</tr>\n<tr>\n<td>Fire On</td>\n<td><code>asx_fireon</code></td>\n<td>Choice → <code>asx_actionfireon</code></td>\n<td>Yes</td>\n<td>On Match / On No Match</td>\n</tr>\n<tr>\n<td>Target Column</td>\n<td><code>asx_targetcolumn</code></td>\n<td>Text</td>\n<td>No</td>\n<td>Set Visible / Set Required target; blank on a form-level Block</td>\n</tr>\n<tr>\n<td>Value</td>\n<td><code>asx_valuebool</code></td>\n<td>Yes/No</td>\n<td>No</td>\n<td>Set Visible: show; Set Required: required</td>\n</tr>\n<tr>\n<td>Apply Inverse When Not Fired</td>\n<td><code>asx_applyinversewhennotfired</code></td>\n<td>Yes/No</td>\n<td>No</td>\n<td>Reserved: not consumed by the current runtime and not shown in the visual editor</td>\n</tr>\n<tr>\n<td>Message</td>\n<td><code>asx_message</code></td>\n<td>Multiline text</td>\n<td>No</td>\n<td>Show Message / Block text (default/fallback; see <code>asx_localizedmessage</code> for per-language overrides)</td>\n</tr>\n<tr>\n<td>Severity</td>\n<td><code>asx_severity</code></td>\n<td>Choice → <code>asx_severity</code></td>\n<td>No</td>\n<td>Notification level</td>\n</tr>\n<tr>\n<td>Target Table</td>\n<td><code>asx_targettable</code></td>\n<td>Text</td>\n<td>No</td>\n<td>Create Record target</td>\n</tr>\n<tr>\n<td>Target Node</td>\n<td><code>asx_targetnode</code></td>\n<td>Lookup → <code>asx_tableconfig</code></td>\n<td>No</td>\n<td>Update / Delete Record target (see <em>Value nodes</em> below)</td>\n</tr>\n<tr>\n<td>Field Mapping</td>\n<td><code>asx_fieldmapping</code></td>\n<td>Multiline text (JSON)</td>\n<td>No</td>\n<td>Create/Update value map</td>\n</tr>\n<tr>\n<td>Order</td>\n<td><code>asx_order</code></td>\n<td>Whole Number</td>\n<td>No</td>\n<td>Execution order</td>\n</tr>\n<tr>\n<td>Is Active</td>\n<td><code>asx_isactive</code></td>\n<td>Yes/No</td>\n<td>No</td>\n<td>Default Yes</td>\n</tr>\n</tbody></table>\n<h2>Value nodes</h2>\n<p><code>asx_comparisonvaluenode</code> (on a condition) and <code>asx_targetnode</code> (on an action) are\nboth lookups into the same <code>asx_tableconfig</code> tree the rule&#39;s conditions traverse.\nBoth must resolve to a <strong>single-cardinality</strong> node: the Root node (the triggering\nrecord) or a Lookup node (one related record), never a Child node.</p>\n<h2>Node filters</h2>\n<p>Node filters narrow which rows at a given <code>asx_tableconfig</code> node participate in\nevaluation: for example, restricting a Row Count condition&#39;s child collection to\nrows matching a criterion, or gating an &quot;at least N related rows exist&quot; check.</p>\n<h3>Node Filter Group (<code>asx_nodefiltergroup</code>)</h3>\n<p>An AND/OR node in a node filter&#39;s tree (self-referential).</p>\n<table>\n<thead>\n<tr>\n<th>Column</th>\n<th>Schema name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Condition Group</td>\n<td><code>asx_conditiongroup</code></td>\n<td>Lookup → <code>asx_conditiongroup</code></td>\n<td>Yes</td>\n<td>Scope</td>\n</tr>\n<tr>\n<td>Table Config Node</td>\n<td><code>asx_tableconfignode</code></td>\n<td>Lookup → <code>asx_tableconfig</code></td>\n<td>No</td>\n<td>Node targeted</td>\n</tr>\n<tr>\n<td>Parent Filter Group</td>\n<td><code>asx_parentfiltergroup</code></td>\n<td>Lookup → <code>asx_nodefiltergroup</code></td>\n<td>No</td>\n<td>Self-referential</td>\n</tr>\n<tr>\n<td>Logical Operator</td>\n<td><code>asx_logicaloperator</code></td>\n<td>Choice → <code>asx_logicaloperator</code></td>\n<td>Yes</td>\n<td>And / Or</td>\n</tr>\n<tr>\n<td>Rule Condition</td>\n<td><code>asx_rulecondition</code></td>\n<td>Lookup → <code>asx_rulecondition</code></td>\n<td>No</td>\n<td>Owning condition of a per-condition filter; null = legacy group-wide</td>\n</tr>\n<tr>\n<td>Owning Criterion</td>\n<td><code>asx_owningcriterion</code></td>\n<td>Lookup → <code>asx_nodefiltercriterion</code></td>\n<td>No</td>\n<td>Exists sub-filter root: this group is the collection&#39;s filter</td>\n</tr>\n</tbody></table>\n<h3>Node Filter Criterion (<code>asx_nodefiltercriterion</code>)</h3>\n<p>A single leaf check inside a Node Filter Group.</p>\n<table>\n<thead>\n<tr>\n<th>Column</th>\n<th>Schema name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Filter Group</td>\n<td><code>asx_filtergroup</code></td>\n<td>Lookup → <code>asx_nodefiltergroup</code></td>\n<td>Yes</td>\n<td>Parent group</td>\n</tr>\n<tr>\n<td>Criterion Type</td>\n<td><code>asx_criteriontype</code></td>\n<td>Choice → <code>asx_criteriontype</code></td>\n<td>No</td>\n<td>Comparison / Exists; default Comparison</td>\n</tr>\n<tr>\n<td>Collection Node</td>\n<td><code>asx_collectionnode</code></td>\n<td>Lookup → <code>asx_tableconfig</code></td>\n<td>No</td>\n<td>Exists only: the child/lookup collection to count</td>\n</tr>\n<tr>\n<td>Min Count</td>\n<td><code>asx_mincount</code></td>\n<td>Whole Number</td>\n<td>No</td>\n<td>Exists only: minimum matching rows (blank = 0)</td>\n</tr>\n<tr>\n<td>Max Count</td>\n<td><code>asx_maxcount</code></td>\n<td>Whole Number</td>\n<td>No</td>\n<td>Exists only: maximum matching rows (blank = unlimited)</td>\n</tr>\n<tr>\n<td>Field Name</td>\n<td><code>asx_fieldname</code></td>\n<td>Text</td>\n<td>Yes</td>\n<td>Column to filter on</td>\n</tr>\n<tr>\n<td>Operator</td>\n<td><code>asx_operator</code></td>\n<td>Text</td>\n<td>Yes</td>\n<td><code>eq</code>, <code>ne</code>, <code>like</code>, <code>not-like</code>, <code>null</code>, <code>not-null</code>, <code>contains</code>, <code>not-contains</code>, <code>gt</code>, <code>ge</code>, <code>lt</code>, <code>le</code></td>\n</tr>\n<tr>\n<td>Value</td>\n<td><code>asx_value</code></td>\n<td>Text</td>\n<td>No</td>\n<td>Literal comparison value</td>\n</tr>\n<tr>\n<td>Comparison Value Source</td>\n<td><code>asx_comparisonvaluesource</code></td>\n<td>Choice → <code>asx_comparisonvaluesource</code></td>\n<td>No</td>\n<td>Blank = Literal; Field Reference compares against another field</td>\n</tr>\n<tr>\n<td>Comparison Value Node</td>\n<td><code>asx_comparisonvaluenode</code></td>\n<td>Lookup → <code>asx_tableconfig</code></td>\n<td>No</td>\n<td>Field Reference right-hand node</td>\n</tr>\n<tr>\n<td>Comparison Value Column</td>\n<td><code>asx_comparisonvaluecolumn</code></td>\n<td>Text</td>\n<td>No</td>\n<td>Field Reference right-hand column</td>\n</tr>\n</tbody></table>\n<h2>Other tables</h2>\n<p><code>asx_searchcriteriagroup</code> / <code>asx_searchcriterion</code> hold the in-memory filter used by\nRow Count conditions. <code>asx_localizedmessage</code> holds per-language overrides of an\naction&#39;s default message.</p>","prevSlug":"troubleshooting","nextSlug":"custom-apis"},"custom-apis":{"title":"Custom APIs","section":"Developer Reference","order":402,"slug":"custom-apis","html":"<p>The engine exposes four <strong>unbound Dataverse Custom APIs</strong> for integrating with rules\noutside the built-in save enforcement and form behavior described in <em>How Rules Run</em>.\nAll four are callable through the standard Dataverse Web API\n(<code>Xrm.WebApi.online.execute</code> from client code, or a plain HTTP request from a\nserver-side integration), and none requires a custom output table. Results come back\nas JSON in the response parameters.</p>\n<h2><code>asx_ValidateRule</code>: validate a rule</h2>\n<p>Validates a <strong>persisted</strong> rule and returns a structured report of what, if anything,\nis wrong with it. This is the check behind the Rule Builder&#39;s status badge and the\ngate on the Draft → Published transition (see <em>Rule Lifecycle</em>). It is <strong>always\nnon-enforcing</strong>: an invalid rule comes back as report data, never as an exception. It\nthrows only on bad input: a missing or non-GUID <code>RuleId</code>, or a rule that doesn&#39;t\nexist.</p>\n<p><strong>Request</strong></p>\n<table>\n<thead>\n<tr>\n<th>Parameter</th>\n<th>Type</th>\n<th>Optional</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>RuleId</code></td>\n<td>String</td>\n<td>No</td>\n<td>GUID of the <code>asx_rule</code> record to validate</td>\n</tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table>\n<thead>\n<tr>\n<th>Parameter</th>\n<th>Type</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>IsValid</code></td>\n<td>Boolean</td>\n<td><code>true</code> when no Error-severity issue was found</td>\n</tr>\n<tr>\n<td><code>Issues</code></td>\n<td>String</td>\n<td>JSON report (see below); <code>&quot;issues&quot;:[]</code> when valid</td>\n</tr>\n</tbody></table>\n<pre><code class=\"language-json\">{\n  &quot;isValid&quot;: false,\n  &quot;issues&quot;: [\n    {\n      &quot;severity&quot;: &quot;Error&quot;,\n      &quot;code&quot;: &quot;STRUCT_NO_CONDITIONS&quot;,\n      &quot;message&quot;: &quot;The rule has no conditions.&quot;,\n      &quot;target&quot;: { &quot;kind&quot;: &quot;Rule&quot;, &quot;id&quot;: &quot;00000000-0000-0000-0000-000000000000&quot;, &quot;field&quot;: null }\n    }\n  ]\n}\n</code></pre>\n<p><code>severity</code> is currently always <code>&quot;Error&quot;</code>; the <code>&quot;Warning&quot;</code> value is defined in the\nmodel but no check in this version emits it. <code>code</code> is a stable machine token;\n<code>target.kind</code> is one of <code>&quot;Rule&quot;</code>, <code>&quot;Group&quot;</code>, <code>&quot;Condition&quot;</code>, or <code>&quot;Action&quot;</code>, and\n<code>target.field</code> names the specific column at fault when the issue targets one.</p>\n<h2><code>asx_RunRules</code>: on-demand evaluation</h2>\n<p>Evaluates the rules engine against a single record (saved, unsaved, or a mix of\nboth) and reports back every action that fired, without writing anything or\nenforcing anything. This is the <strong>Manual</strong> trigger&#39;s API described in <em>How Rules\nRun</em>, and what the client form library round-trips to for every rule it\nevaluates on a form. See <em>Client Form Library</em>.</p>\n<p><strong>Request</strong></p>\n<table>\n<thead>\n<tr>\n<th>Parameter</th>\n<th>Type</th>\n<th>Optional</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>TableName</code></td>\n<td>String</td>\n<td>No</td>\n<td>Logical name of the record&#39;s table</td>\n</tr>\n<tr>\n<td><code>RecordId</code></td>\n<td>String</td>\n<td>Yes</td>\n<td>GUID of an existing record</td>\n</tr>\n<tr>\n<td><code>RecordJson</code></td>\n<td>String</td>\n<td>Yes</td>\n<td>Unsaved field values as a flat JSON object <code>{ &quot;&lt;logicalname&gt;&quot;: &lt;value&gt; }</code></td>\n</tr>\n<tr>\n<td><code>Triggers</code></td>\n<td>String</td>\n<td>Yes</td>\n<td>Single trigger name; defaults to <code>Manual</code></td>\n</tr>\n<tr>\n<td><code>IncludeDiagnostics</code></td>\n<td>Boolean</td>\n<td>Yes</td>\n<td>When <code>true</code>, the response also carries <code>Diagnostics</code> (timings and fetch counts for this evaluation). Default <code>false</code></td>\n</tr>\n</tbody></table>\n<p>At least one of <code>RecordId</code> / <code>RecordJson</code> is required. Supplying both retrieves the\npersisted record and overlays the JSON fields on top of it.</p>\n<p><code>RecordJson</code> values are encoded per attribute kind: a lookup is\n<code>{ &quot;id&quot;: &quot;&lt;guid&gt;&quot;, &quot;logicalname&quot;: &quot;&lt;table&gt;&quot; }</code>; a multi-select choice is an array of\nintegers; a whole number, single-select choice, or status is a plain integer; a\ndecimal or money value is a fractional number; a boolean is <code>true</code>/<code>false</code>; a date is\nan ISO-8601 string; and <code>null</code> clears/omits the attribute.</p>\n<p><strong>Response</strong></p>\n<table>\n<thead>\n<tr>\n<th>Parameter</th>\n<th>Type</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>IsValid</code></td>\n<td>Boolean</td>\n<td><code>true</code> when no <code>Block</code> action fired</td>\n</tr>\n<tr>\n<td><code>FailedRuleCount</code></td>\n<td>Integer</td>\n<td>Count of distinct rules with a fired <code>Block</code> action</td>\n</tr>\n<tr>\n<td><code>Results</code></td>\n<td>String</td>\n<td>JSON array of every fired action</td>\n</tr>\n<tr>\n<td><code>Diagnostics</code></td>\n<td>String</td>\n<td>Only when <code>IncludeDiagnostics</code> was <code>true</code>: a JSON object describing the evaluation (see below)</td>\n</tr>\n</tbody></table>\n<pre><code class=\"language-json\">[\n  {\n    &quot;ruleId&quot;: &quot;00000000-0000-0000-0000-000000000000&quot;,\n    &quot;actionType&quot;: &quot;Block&quot;,\n    &quot;fireOn&quot;: &quot;OnNoMatch&quot;,\n    &quot;targetColumn&quot;: null,\n    &quot;value&quot;: null,\n    &quot;message&quot;: &quot;Localized message text&quot;,\n    &quot;severity&quot;: &quot;Error&quot;,\n    &quot;targetTable&quot;: null\n  }\n]\n</code></pre>\n<p>Enums serialize as string names; fields irrelevant to a given action type are\n<code>null</code>. A fired <code>CreateRecord</code> / <code>UpdateRecord</code> / <code>DeleteRecord</code> action also carries\na <code>write</code> object, the fully-resolved write intent (<code>operation</code>, <code>targetTable</code>,\n<code>targetId</code>, <code>values</code>). <code>asx_RunRules</code> reports that intent; only the server engine\napplies it, on Create/Update/Delete. See <em>Runtime Enforcement</em>.</p>\n<p><strong>Diagnostics</strong> (opt-in, <code>IncludeDiagnostics: true</code>) report what the evaluation cost,\nfor support conversations and your own sizing against the <em>Beta Limitations</em> budget:</p>\n<pre><code class=\"language-json\">{\n  &quot;totalMs&quot;: 412,\n  &quot;rulesLoaded&quot;: 12, &quot;rulesEvaluated&quot;: 12, &quot;rulesFired&quot;: 1,\n  &quot;retrieveCount&quot;: 3, &quot;retrieveMultipleCount&quot;: 4, &quot;rowsFetched&quot;: 260,\n  &quot;stages&quot;: [ { &quot;name&quot;: &quot;loadRules&quot;, &quot;ms&quot;: 40 }, { &quot;name&quot;: &quot;queryExecute&quot;, &quot;ms&quot;: 310 }, { &quot;name&quot;: &quot;evaluate&quot;, &quot;ms&quot;: 62 } ],\n  &quot;nodes&quot;:  [ { &quot;nodeId&quot;: &quot;…&quot;, &quot;table&quot;: &quot;sample_orderline&quot;, &quot;retrieveCount&quot;: 0, &quot;retrieveMultipleCount&quot;: 2, &quot;rows&quot;: 240 } ]\n}\n</code></pre>\n<p><code>stages</code> are the engine&#39;s internal phases; the names may change between releases, so\ntreat them as labels, not an API. <code>nodes</code> is one entry per traversed configuration\nnode. The numbers are server-side evaluation cost only, not end-user save latency.</p>\n<h2><code>asx_ReadRules</code>: runtime projection</h2>\n<p>Returns the assembled rule definitions for a table, serialized to JSON: triggers,\nthe condition-group tree, the Table Config nodes each condition binds to, and the\nactions. It performs <strong>no evaluation</strong>: where <code>asx_RunRules</code> answers &quot;what fired for\nthis record?&quot;, <code>asx_ReadRules</code> answers &quot;what are the rules for this table?&quot;. It is\nmodeled as a Dataverse <strong>Function</strong>, so it is callable with an HTTP <code>GET</code>. The client\nform library calls it once per table on form load to discover which columns to watch\nand which rules to evaluate.</p>\n<p><strong>Request</strong></p>\n<table>\n<thead>\n<tr>\n<th>Parameter</th>\n<th>Type</th>\n<th>Optional</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>TableName</code></td>\n<td>String</td>\n<td>No</td>\n<td>Logical name of the table whose rules to return</td>\n</tr>\n<tr>\n<td><code>Triggers</code></td>\n<td>String</td>\n<td>Yes</td>\n<td>Single trigger name; defaults to <code>OnForm</code></td>\n</tr>\n</tbody></table>\n<p><code>asx_ReadRules</code> is record-agnostic, so there&#39;s no <code>RecordId</code>/<code>RecordJson</code> parameter.\nIt returns only the rules that would actually apply: <strong>Published</strong> status, the\nrequested trigger, a matching Channel (see <em>Triggers &amp; Channels</em>), and within the\nrule&#39;s effective window (see <em>Rule Lifecycle</em>). When nothing matches, the response is\na well-formed envelope with an empty <code>rules</code> array, not an error.</p>\n<p><strong>Response</strong></p>\n<table>\n<thead>\n<tr>\n<th>Parameter</th>\n<th>Type</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>Rules</code></td>\n<td>String</td>\n<td>JSON envelope: <code>{ tableLogicalName, languageId, rules: [...] }</code></td>\n</tr>\n</tbody></table>\n<pre><code class=\"language-jsonc\">{\n  &quot;tableLogicalName&quot;: &quot;account&quot;,\n  &quot;languageId&quot;: 1033,\n  &quot;rules&quot;: [\n    {\n      &quot;ruleId&quot;: &quot;00000000-0000-0000-0000-000000000000&quot;,\n      &quot;name&quot;: &quot;Credit limit approver required for large accounts&quot;,\n      &quot;triggers&quot;: [&quot;OnForm&quot;, &quot;OnCreate&quot;],\n      &quot;conditionGroups&quot;: [\n        /* logicalOperator, isExecutionCondition, conditions[], groups[]. Each condition&#39;s\n           conditionType is one of FieldComparison / RowCount / RegexMatch / Calculation, and\n           valueSource is one of Literal / FieldReference / Template / DateExpression\n           (illustrative, not exhaustive; see Schema Reference for the authoritative list) */\n      ],\n      &quot;tableConfig&quot;: [ /* the Table Config nodes this rule references */ ],\n      &quot;actions&quot;: [ /* actionType, fireOn, targetColumn, value, message, severity, order, ... */ ]\n    }\n  ]\n}\n</code></pre>\n<p>Field names are camelCase, enums are string names, and <code>message</code> text is already\nlocalized to the caller&#39;s UI language (<code>languageId</code> in the envelope). Write actions\nadd <code>targetTable</code>, <code>targetNode</code>, and <code>fieldMapping</code>.</p>\n<h2><code>asx_SyncSteps</code>: reconcile or remove the generated steps</h2>\n<p><code>asx_SyncSteps</code> is the manual control over the generated enforcement steps (see\n<em>Plugin Registration</em>), for the two cases the automatic reconciliation does not\ncover: recovering from drift, and clearing up before an uninstall.</p>\n<p>It is gated on the <strong>calling</strong> user holding <code>prvWriteSdkMessageProcessingStep</code>. The\nstep changes themselves are made as the system user, the same as during a publish.</p>\n<p><strong>Request</strong></p>\n<table>\n<thead>\n<tr>\n<th>Parameter</th>\n<th>Type</th>\n<th>Optional</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>Mode</code></td>\n<td>String</td>\n<td>Yes</td>\n<td><code>Sync</code> (the default when omitted or empty) or <code>RemoveAll</code>. Case-insensitive. Any other value fails the call</td>\n</tr>\n</tbody></table>\n<p><code>Mode = &quot;Sync&quot;</code> walks every table that has rules and reconciles its steps against the\ncurrent rule configuration: it registers what is missing, widens what is too narrow,\nand deletes steps the rules no longer call for. Use it after a drift-inducing change,\nsuch as a step edited or removed by hand. To repair a single table instead, deactivate\nand reactivate any one rule on it (see <em>Plugin Registration</em>).</p>\n<p><code>Mode = &quot;RemoveAll&quot;</code> deletes every step the engine owns. This is the pre-uninstall\nstep: the solution cannot be removed while its generated steps still reference it\n(see <em>Installing, Verifying &amp; Uninstalling</em>). Enforcement stops immediately, so on a\nlive environment treat it as an outage rather than routine maintenance. Publishing a\nrule, or a later <code>Sync</code>, regenerates the steps.</p>\n<p>Neither mode re-enables a step an administrator deactivated. Deactivated steps are\ncounted, reported, and left as they are, so a repair does not restore enforcement\nthat was deliberately suspended (<em>Troubleshooting</em>).</p>\n<p><strong>Response</strong></p>\n<table>\n<thead>\n<tr>\n<th>Property</th>\n<th>Type</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>TablesProcessed</code></td>\n<td>Integer</td>\n<td>Tables examined</td>\n</tr>\n<tr>\n<td><code>StepsCreated</code></td>\n<td>Integer</td>\n<td>Steps registered</td>\n</tr>\n<tr>\n<td><code>StepsUpdated</code></td>\n<td>Integer</td>\n<td>Existing steps widened or corrected</td>\n</tr>\n<tr>\n<td><code>StepsDeleted</code></td>\n<td>Integer</td>\n<td>Steps removed</td>\n</tr>\n<tr>\n<td><code>DeactivatedStepsFound</code></td>\n<td>Integer</td>\n<td>Deactivated steps seen and deliberately left alone</td>\n</tr>\n<tr>\n<td><code>Details</code></td>\n<td>String</td>\n<td>JSON array, one entry per table where something actually changed</td>\n</tr>\n</tbody></table>\n<p><code>Details</code> names the steps rather than only counting them:</p>\n<pre><code class=\"language-json\">[\n  {\n    &quot;table&quot;: &quot;sample_order&quot;,\n    &quot;created&quot;: [&quot;Ascentix.RulesEngine: sample_order Create&quot;],\n    &quot;updated&quot;: [],\n    &quot;deleted&quot;: [],\n    &quot;deactivated&quot;: [&quot;Ascentix.RulesEngine: sample_order Update&quot;]\n  }\n]\n</code></pre>\n<p>Tables where nothing changed are omitted, so an empty array means everything was\nalready correct.</p>","prevSlug":"schema-reference","nextSlug":"client-form-library"},"client-form-library":{"title":"Client Form Library","section":"Developer Reference","order":403,"slug":"client-form-library","html":"<p><code>asx_rulesengine.js</code> is the JavaScript web resource that gives rules their on-form\nbehavior with no custom JavaScript of your own: the <em>On form (client, advisory)</em> path\ndescribed in <em>Runtime Enforcement</em>. The client is advisory only. The server plugin is\nthe authoritative enforcer of every rule, and everything below assumes that.</p>\n<h2>What it does</h2>\n<p>On form load, the library:</p>\n<ol>\n<li>Calls <code>asx_ReadRules</code> once for the form&#39;s table (requesting the <code>OnForm</code>\ntrigger), caches the rule definitions, and derives which columns the rules depend\non and which columns/messages the rules can target.</li>\n<li>Snapshots the baseline visibility and required-level of every control the rules\nmight touch.</li>\n<li>Registers a change handler on each dependency column present on the form. It\nregisters <strong>no</strong> save handler: the client never blocks or triggers a save.</li>\n<li>Runs an initial evaluate-then-apply pass.</li>\n</ol>\n<p>Each subsequent evaluate-then-apply cycle (on load, and again on every relevant field\nchange) serializes the current form values for the dependency columns, calls\n<code>asx_RunRules</code> for the table with those values, resets every previously-touched\ncontrol back to its baseline, and applies the actions the response reports as fired.\nSee <em>Custom APIs</em>.</p>\n<h2>Action mapping</h2>\n<table>\n<thead>\n<tr>\n<th>Action Type</th>\n<th>Form effect</th>\n<th>Blocks save?</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Set Visible</td>\n<td>Shows/hides the target control</td>\n<td>No</td>\n</tr>\n<tr>\n<td>Set Required</td>\n<td>Sets the target field&#39;s required level</td>\n<td>An empty required field blocks natively through the platform&#39;s own validation</td>\n</tr>\n<tr>\n<td>Show Message (form-level)</td>\n<td>Sets a form notification at the action&#39;s configured severity</td>\n<td>No</td>\n</tr>\n<tr>\n<td>Show Message (field-targeted)</td>\n<td>Adds a notification to the control</td>\n<td>Yes. The platform renders control notifications at Error only, and an Error control notification stops the save through native field validation, whatever severity the action carries</td>\n</tr>\n<tr>\n<td>Block (field-targeted)</td>\n<td>Adds an inline notification on that field only</td>\n<td>Yes, via the platform&#39;s own field validation: it rolls the field notification up to the form header on save and blocks there</td>\n</tr>\n<tr>\n<td>Block (form-level)</td>\n<td>Shows a form banner</td>\n<td>No (no field for the platform to roll a notification up from)</td>\n</tr>\n<tr>\n<td>Create/Update/Delete Record</td>\n<td>Ignored on the client</td>\n<td>n/a (server-only)</td>\n</tr>\n<tr>\n<td>Bulk imports, API calls, and other writes that never pass through this form are</td>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td>gated on save by the server plugin regardless of what the client shows.</td>\n<td></td>\n<td></td>\n</tr>\n</tbody></table>\n<h2>Wiring it onto a form</h2>\n<p>On each model-driven form that should run the rules engine:</p>\n<ol>\n<li>Add the <code>asx_rulesengine</code> web resource as a <strong>form library</strong>.</li>\n<li>Register one event handler:<ul>\n<li><strong>Event:</strong> <code>OnLoad</code></li>\n<li><strong>Function:</strong> <code>Ascentix.RulesEngine.onLoad</code></li>\n<li><strong>Pass execution context as first parameter:</strong> checked</li>\n</ul>\n</li>\n</ol>\n<p>That is the only handler to register by hand. From inside <code>onLoad</code> the library\nself-registers change handlers on the dependency columns it discovers from\n<code>asx_ReadRules</code>.</p>\n<blockquote>\n<p><code>asx_authoringforms.js</code> is a separate bundle: it drives conditional visibility on\nthe engine&#39;s own configuration forms inside the Rules Engine app, not rules running\non your own tables.</p>\n</blockquote>\n<h2>Graceful degradation</h2>\n<p>The library never breaks or freezes a form on its own failure:</p>\n<ul>\n<li>If <code>asx_ReadRules</code> fails on load (network error, the caller lacks read access, or\nthe API isn&#39;t available), the library logs the error, skips all wiring, and leaves\nthe form usable as-is with no end-user error banner.</li>\n<li>If <code>asx_RunRules</code> fails mid-cycle, the library logs the error and keeps the last\nsuccessful cycle&#39;s state rather than wiping the form or fabricating a block\nnotification.</li>\n</ul>","prevSlug":"custom-apis","nextSlug":"plugin-registration"},"plugin-registration":{"title":"Plugin Registration","section":"Developer Reference","order":404,"slug":"plugin-registration","html":"<p>The server-side enforcement described in <em>Runtime Enforcement</em> is implemented as a\nDataverse plugin, but you don&#39;t register that plugin against your tables by hand.</p>\n<h2>Two plugins that matter for enforcement registration</h2>\n<p>The engine ships one signed plugin assembly.</p>\n<ul>\n<li><strong><code>RulesEnginePlugin</code></strong>: the evaluator and enforcer (see <em>Runtime Enforcement</em>).\nIts steps against your own tables are <strong>generated at runtime</strong>, not shipped as part\nof the solution.</li>\n<li><strong><code>RuleRegistrationPlugin</code></strong>: keeps those generated steps in sync with your rule\nconfiguration. This is the one piece that <em>is</em> shipped and registered up front.</li>\n</ul>\n<h2>Shipped bootstrap steps</h2>\n<p><code>RuleRegistrationPlugin</code> is registered once, as pre-operation synchronous steps, on\nthe engine&#39;s own configuration tables:</p>\n<table>\n<thead>\n<tr>\n<th>Table</th>\n<th>Messages</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>asx_rule</code></td>\n<td>Create, Update, Delete</td>\n</tr>\n<tr>\n<td><code>asx_ruleaction</code></td>\n<td>Create, Update, Delete</td>\n</tr>\n</tbody></table>\n<p>These six <code>RuleRegistrationPlugin</code> steps travel with the solution, as does the\npublish gate and the revision guards described below. No filtering\nattributes are applied to the six: every create, update, and delete against a\nrule or an action passes through <code>RuleRegistrationPlugin</code>.</p>\n<h2>Generated steps</h2>\n<p>Whenever you create, edit, activate, or delete a rule, <code>RuleRegistrationPlugin</code>\nreconciles the corresponding <code>RulesEnginePlugin</code> step on <strong>your</strong> table: creating it\nif it doesn&#39;t exist, updating its filtering/message registration if the rule\nchanged, or removing it when the rule no longer targets that message. Delete every\nrule on a table and that table&#39;s generated steps go with them.</p>\n<p>Generated steps are named <code>Ascentix.RulesEngine: {table} {message}</code> (for example,\n<code>Ascentix.RulesEngine: account Update</code>) and are visible like any other step in the\nPlug-in Registration Tool. Where the environment supports it, they also cover\nDataverse&#39;s bulk <code>CreateMultiple</code> / <code>UpdateMultiple</code> messages.</p>\n<h2>Publish gate</h2>\n<p>A rule only enforces once it&#39;s <strong>Published</strong> (see <em>Rule Lifecycle</em>). A shipped step\non <code>asx_rule</code> runs the same validation as <em>Validating &amp; Publishing</em> and blocks the\nDraft → Published transition if the rule is invalid.</p>\n<p>Every explicit publication, including republishing a live rule, captures the saved\ndraft and its data model. Configuration guards execute first (order 1), the\npublisher next (20), and registration reconciliation last (30), all synchronously\nin pre-operation. Guards cover Create/Update/Delete of the ten configuration tables\nand revision table, plus legacy SetState on rules. Draft edits preserve the\nregistrations required by the published snapshot.</p>\n<h2>Drift repair</h2>\n<p>If steps drift out of sync with your rules (for example, if rules were edited while\nthe registration plugin was temporarily disabled), deactivate and then reactivate any\none rule on the affected table. Reconciliation is table-scoped, so that single save\nre-syncs every generated step for the whole table. To reconcile every table in one\ncall instead, use <code>asx_SyncSteps</code> with <code>Mode = &quot;Sync&quot;</code> (see <em>Custom APIs</em>).</p>","prevSlug":"client-form-library","nextSlug":null}},"firstSlug":"what-is-it"};
