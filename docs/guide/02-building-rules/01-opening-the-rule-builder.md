---
title: Opening the Rule Builder
section: Building Rules
order: 201
slug: opening-the-rule-builder
screenshots:
  - file: images/02-01-opening-the-rule-builder-01.png
    caption: The Rule Builder opens from the app sitemap under Authoring → Visual Rule Editor.
    alt: Model-driven app sitemap with an Authoring group ("Visual Rule Editor" selected) above a Configuration group (Rules, Table Configs), and a "Rule Configuration" area switcher at the bottom.
---

# Opening the Rule Builder

The **Rule Builder** is the full-page visual editor for rules: conditions,
actions, and table configuration.

## Sitemap

Open the model-driven app and use the **Authoring** group in the sitemap,
under the **Visual Rule Editor** subarea.

![Model-driven app sitemap with an Authoring group ("Visual Rule Editor" selected) above a Configuration group (Rules, Table Configs), and a "Rule Configuration" area switcher at the bottom.](../images/02-01-opening-the-rule-builder-01.png)

Selecting it opens the **hub** (the "Rules & data model" landing page), where
you work with the rules and table configurations that already exist. See
*The Hub* for what's on that page.

> The editor only runs inside the app frame: it relies on the app's client
> API for its data access. Don't try to open it as a bare standalone page;
> it will load but won't be able to read or save anything.
